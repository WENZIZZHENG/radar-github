# Radar 雷达环境变量模板（git 只维护这一套）：本地复制为 .env.dev，生产复制为 .env.prod。
#   cp .env.example .env.dev     # 本地开发
#   cp .env.example .env.prod    # 生产（部署到服务器 /opt/radar/，并删掉下面两个 =0 开关）
# .env.dev / .env.prod 都被 .gitignore 排除、永不入仓；本模板只列键名与说明，不放任何真实值。
# 加载口径（app/config.py）：代码只读 .env.dev；生产由 radar.service 的 EnvironmentFile 注入，
# 系统环境变量永远优先于文件（setdefault 不覆盖）。
# 本模板默认姿态 = 本地安全（AI 与定时任务默认关闭）：生产实例化时必须删掉这两行 =0。

# GitHub 个人访问令牌（PAT），每日采集用——GraphQL nodes 批量 + Search。
# 只需公开数据只读权限（public_repo 足够）；缺失时采集任务在使用点报错，榜单页面不受影响。
GITHUB_TOKEN=

# ===== AI 提供方（OpenAI 兼容 chat/completions 协议；缺省 DeepSeek，换提供方只改本节变量） =====

# AI 总开关：0 = 不起真实 AI 调用、不烧额度（本地开发默认姿态），
# 搜索/翻译/推荐语/概要/候选词扫描全链路自动降级（榜单照常），与 key 缺失同一降级姿态。
# 生产实例化 .env.prod 时删掉本行（缺省 1 开）。
AI_ENABLED=0

# AI API key：翻译 / 推荐理由 / AI 概要 / 智能搜索 / 候选词扫描。
# 缺失或 AI_ENABLED=0 时 AI 全链路自动降级（榜单照常出，无翻译/推荐语），不阻塞主流程。
AI_API_KEY=
# 兼容键（旧版键名，AI_API_KEY 未配置时回退读取；存量配置无需改名）：
# DEEPSEEK_API_KEY=

# 提供方端点与模型（可选，缺省 DeepSeek 官方）：换 OpenAI 兼容提供方时改这两个。
# SDK 语义：base_url 写到 /v1 即可，SDK 自动拼 /chat/completions；不要写完整 /v1/chat/completions 路径。
# AI_BASE_URL=https://api.deepseek.com/v1
# AI_MODEL=deepseek-chat

# AI 调用写死参数的缺省覆盖（可选，一般不配；非法值自动回落缺省）：
# AI_TIMEOUT_SECONDS=60        # 单次调用超时（秒）
# AI_MAX_RETRIES=1             # 失败重试次数（重试会放大额度消耗）
# AI_README_HEAD_CHARS=8000    # README 截断入 prompt 的字符上限——单次调用 token 成本主杠杆，降本可砍到 2000~3000

# 定时任务开关：0 = 关掉进程内每日采集调度（本地开发默认姿态，避免起服务顺带打 API）。
# 生产实例化 .env.prod 时删掉本行（缺省开；每日采集/周报都靠它，生产千万不要置 0）。
RADAR_JOBS_ENABLED=0

# SQLite 库路径（可选，缺省 <项目根>/data/radar.db）；生产为 /opt/radar/data/radar.db。
# RADAR_DB_PATH=

# 服务器侧另有两个备份变量（BACKUP_DIR / BACKUP_REMOTE），不走 dotenv——
# cron 环境不读它，直接写在 crontab 里，见 scripts/backup.sh 头注释。

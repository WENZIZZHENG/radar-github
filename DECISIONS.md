# DECISIONS.md（项目规范变更记录）

规范文档（AGENTS.md）条款变更的原因与旧文记录处；正文只保留现行口径，沿革集中在这里。

## 2026-08-18：引入 OpenSpec 作为技术契约体系

**旧文**（§1"技术契约体系"）：
> 技术契约体系：无（S 档单人项目，不引入 OpenSpec；技术契约以《共识文档》+《架构决策记录》为准）。

**新文**：见 AGENTS.md §1"技术契约体系"现行条款（OpenSpec 管技术变更过程物，`docs/sop/` 仍为产品层决策唯一事实来源）。

**原因**：本人在"榜单新项目区"功能（首个 OpenSpec change：`add-new-project-zone`）的记录过程中拍板引入。grilling 共识需要结构化的提案/设计/规格/任务载体，OpenSpec 四件产物正好承接；S 档"不引入"的原判断被本人推翻。

**附带决定**：快速模式小修不强制 OpenSpec；验收通过后 `openspec archive` 归档，产品层口径回写 `docs/sop/` 冻结物，两边不打架。

## 2026-08-19：§3"子 agent 模型路由"合并入全局 §5

**旧文**（§3 全节，标题含"本人拍板，优先级高于全局同名条款"）：
> - 实施类全部 DeepSeek（secondary）起手；评审一律 k3（primary）。
> - 每任务评审最多三轮（k3 初审→DeepSeek 修→k3 复审→DeepSeek 修→k3 三审）；三轮后仍有未清零的中/高 findings 则 k3 接手**该任务**（不牵连后续任务，下一任务仍 DeepSeek 起手）。
> - 低 findings 可留痕不修，不算"不过"。
> - 图形/视觉判断（截图判读、视觉核对）一律 k3：DeepSeek 无 image_in，不做看图环节。

**新文**：§3 改为"按全局 `AGENTS.md` §5 口径，本项目无额外加严或覆盖"；§4 中"独立评审子 agent 一律 k3"改为指向 §3 的引用。全局 §5 正文未动（旧文四条与全局 §5 现行条款逐条等价，无内容需并入）。

**原因**：项目 §3 与全局 §5 内容完全重复，违反"同一规则只在一处展开"；应本人要求合并，单点收敛到全局 §5，项目侧只留引用。全局 §5 本就是更完整的版本（含 fallback、验证环节、thinking 强度核验等细则），合并后执行口径不变。附带语义变化：旧标题"优先级高于全局同名条款"删除后，此后全局 §5 的变更自动适用于本项目。

## 2026-08-20：AI 重生节奏降频（半月窗口）＋README 截断 3000

**旧文**（`recommend_missing`/`ensure_daily_ai` docstring 口径）：
> 季榜推荐语季内每周一重生（generated_week ≠ 当周 → REPLACE）；总榜推荐语＋概要 README sha 变更当日重生（自愈）；README 截断入 prompt 上限 8000 字符。

**新文**：重生（REPLACE）统一收敛到每月 1 号、15 号的跑批窗口；补缺（缺失生成）不受窗口限制、每日照常；周榜维度不动（周一全量重生维持）；README 截断入 prompt 上限 3000（三处 env 显式配置，代码默认值不动）。技术规格见 OpenSpec change `ai-refresh-cadence`。

**原因**：2026-08-20 DeepSeek 余额烧光事故复盘——实测每周 AI 调用 1500~2000 次，其中 README 变更当日重生（total 段 440~670 次/周＋概要 623~803 次/周）是最大头，README 高频无效变更（徽章/错别字/星数盾）不该触发重生。grilling 共识 Q1~Q4 逐问拍板：截断 3000（零代码纯配置先拿 60% 降幅）、重生改半月窗口（自愈意图保留，灵敏度放宽到半月，与产品周更调性一致）、季榜并入同节奏、周榜不动（核心新鲜度）。综合成本预计降至原来 10~15%。存量文本不重生（方向是省未来的钱，不是统一口径——重生等于花钱把 8000 字符生成的好文本覆盖成 3000 字符的）。

## 2026-08-23：Git 提交节奏上收全局＋§3 悬空引用修正

**旧文**（§5"Git 与文档提交节奏"全节两条）：
> - commit message 用 Conventional Commits：`feat:` / `fix:` / `docs:` / `chore:` / `refactor:` / `test:` 开头，中文描述；历史 commit 不回刷。
> - 文档提交节奏：代码 commit 后，文档改动不立即单独 commit——任务状态/验证留痕仍当场写入文件，但 git 提交等本人反馈确认后与后续改动一起提交。

**新文**：§5 改为纯引用——"按全局 AGENTS.md §3'commit message 与文档提交节奏'段口径执行，本项目无额外加严或覆盖"；全局 §3 新增该段（六前缀、中文描述、历史不回刷、文档攒批四要素逐字等价上收）。

**原因**：本人拍板上收（2026-08-23）——这两条本是全项目通用习惯，只写在项目级会造成多处复制漂移；全局单点、项目引用。

**附带修正**：§3 原写"按全局 AGENTS.md §5 口径"，全局 §5 已于 2026-08-20 整节删除（子 agent 回归继承主会话模型，见全局 DECISIONS.md），引用悬空；§3 改直述现行口径"子 agent 一律继承主会话模型，无模型路由规则"。hook `commit-guard.mjs` 注释中陈旧的"项目级 AGENTS.md §6"引用同批改指全局 §3。

# 任务清单：智能搜索（smart-search）

> 流程口径：任务 1.1《交互流程说明》更新需本人确认后再派实施；外部 API 实时接线 + 新功能 → A 级预演；实施派 DeepSeek，评审派 k3。

## 1. 文字层

- [x] 1.1 更新《交互流程说明》：搜索页入口/布局、意图透明文案、空结果说明措辞、"池外"徽标与关注星交互 → 本人确认（2026-08-18 §17 v2.7 已确认）

## 2. 检索链路（新模块，如 app/search.py）

- [x] 2.1 LLM 意图理解：prompt 与 JSON 契约（多关键词×多语言×主题词，语言取值对齐 classify.LANGUAGES）；非法 JSON 退化为原输入单关键词
- [x] 2.2 池内召回：alive 仓 LIKE 关键词组匹配（full_name/description_en/topics）+ 语言过滤交集，命中数→最新星数粗排取约 30 候选
- [x] 2.3 LLM 精选：候选 30 → 至多 10 条 + 每条约 50 字中文推荐理由（编号引用防幻觉）；精选失败退化粗排直出（无理由）记 WARNING
- [x] 2.4 池外补搜：池内不足 10 条时用同组关键词调 `search_repositories` 补足；结果模型带 `external` 标记；失败/限速/token 缺失跳过补搜
- [x] 2.5 降级：DeepSeek key 未配置/鉴权失败/调用失败 → 明确"暂不可用"提示，主链路不受影响

## 3. 页面与接线（app/web/）

- [x] 3.1 路由：搜索页 GET + 搜索执行（异步/加载态），意图透明区渲染
- [x] 3.2 search.html：输入框 + 结果列表复用 `_row.html`，行下推荐理由段；池外徽标；空结果如实说明
- [x] 3.3 池外关注星：复用 `ingest_followed_repo` 接线；已入池/已关注行不显示关注入口；关注后行态变更
- [x] 3.4 导航栏加"搜索"入口

## 4. 测试

- [x] 4.1 意图理解：JSON 契约解析、非法 JSON 退化、多关键词/多语言产出
- [x] 4.2 池内召回：命中计数、语言过滤、粗排顺序、不足 30/零候选
- [x] 4.3 精选：正常 10 条+理由、精选失败粗排兜底、超范围编号拒绝
- [x] 4.4 池外补搜：补足到 10、池内在前池外在后、补搜失败降级、空结果如实说明
- [x] 4.5 关注闭环：池外仓入池、已在池不显示关注入口
- [x] 4.6 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` 一次跑绿（375 passed：基线 345 + 新增 30）

## 5. 评审与交付

- [x] 5.1 k3 独立评审，中/高 findings 清零（最多三轮）（初审 F2×1：`_github_query` 布尔运算符超 GitHub 单查询上限——已修（关键词截前 4、语言截前 2＋补测试），复审清零；F3×7 留痕不修）
- [x] 5.2 部署生产 + A 级预演（真实 DeepSeek key + 真实 GitHub Search 各跑一次真实查询，含空结果与补搜失败注入核对）+ 复验步骤卡 → 本人走查（预演全绿：真实"我想做爬虫，用 Python"池内 8＋池外 2、空结果如实说明、坏 key 降级注入恢复；本人走查通过 2026-08-19）
- [x] 5.3 通过后当场归档（任务拆解表 + openspec archive），文档按 §5 节奏提交

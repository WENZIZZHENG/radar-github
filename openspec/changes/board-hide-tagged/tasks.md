# 任务清单：榜单"隐藏已打标"过滤

> 流程口径（项目 SOP）：文字层先行——任务 1.1《交互流程说明》更新需本人确认后再派实施；视觉为既有组件复用，免示意图档，开发完以真实页面截图交付走查。

## 1. 文字层

- [x] 1.1 更新《交互流程说明》：榜单章节加 hide_tagged 开关流程（位置、互跳、计数提示、空态、参数透传口径）→ 本人确认

## 2. 服务端（app/web/routes.py）

- [x] 2.1 三口径路由（weekly/quarterly/total）接 `hide_tagged` 参数：仅 `=1` 为开，其余静默按关
- [x] 2.2 展示装配层过滤：`_boards_context`/`_board_view` 路径上丢弃 `tags` 非空的行视图（主榜 rows／新项目区 fresh／新崛起区 rising_rows 三区统一），统计隐藏行数 N；首期降级判定跑在过滤前 boards 上不受影响
- [x] 2.3 页内导航透传：开关开启时 `_sidebar_items` 边栏项、`_switcher` 期次分段控件、开关互跳链接均携带 `hide_tagged=1`（含 `week=`/`board=` 组合）；关闭态链接不带该参数

## 3. 模板（app/web/templates/）

- [x] 3.1 boards.html：开关元素（开/关互跳 GET 链接，文案按 1.1 定稿）＋开启且 N>0 时"已隐藏 N 个已打标项目"提示行
- [x] 3.2 _board.html：某榜三区行全被隐藏时渲染榜块空态小字（榜头保留、区头不渲染）；榜头/区头/边栏计数徽标保持过滤前口径

## 4. 测试

- [x] 4.1 新增：开/关行为（缺省、=1、非法值静默关）、三区各自过滤、隐藏计数按行计、N=0 不渲染提示
- [x] 4.2 新增：榜块全隐藏空态、首期降级判定不受隐藏影响、dead 仓有标签同样隐藏
- [x] 4.3 新增：参数透传（边栏/分段控件/开关互跳，历史周 week= 与 board= 组合）
- [x] 4.4 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` 一次跑绿

## 5. 评审与交付

- [ ] 5.1 独立评审子 agent，中/高 findings 清零（最多三轮）
- [ ] 5.2 起服务真实页面截图核对（开/关、计数提示、榜块空态、跨期次导航不丢开关）＋复验步骤卡 → 本人走查
- [ ] 5.3 部署生产并冒烟（榜单四页 hide_tagged=1 各刷一次）
- [ ] 5.4 通过后当场归档（任务拆解表主表行＋openspec archive），文档按 §5 节奏提交

# 任务拆解表（阶段⑤冻结物）

> 原子任务硬约束：一次会话能做完、可独立提交、精确到文件路径、自带验收标准。
> 风险级：高风险逐个验收；低风险可打包（每包 ≤3 个、同功能内相邻）。本表打包组：P1={T-008, T-010}（页面与标签，同功能相邻，T-009 高风险夹在中间故不打包，两任务在 T-009 前后顺序执行、单独成组验收）。P2={T-022, T-023}（标签小件，同功能相邻）。其余低风险任务逐个验收。
> 确认状态：【已确认】　确认日期：2026-08-09　版本：v1　基线：dev-sop v1.4

## 当前速览
- **批次十四（2026-09-13 立项，本人拍板三条口径）进行中**：T-040 本地 AI 生成回传（导出/回填接口＋人工行保护）——起因＝生产 AI 段按本人要求停用（2026-09-12 `AI_ENABLED=0`），文本改由本人在本地对话式工具生成、经接口回填；口径三条＝生成范围照旧（S 全集五类文本）、周榜维持每周重生、人工行不被自动路径覆盖。OpenSpec change `local-ai-relay` 四件产物已出（`validate --strict` 过、全仓 6 项 0 失败）；文字层 §20 专章＋§8.4 回写已起草（待本人确认）。风险级＝高（动数据结构＋改已验收功能＋新增外部接口），逐个验收。
- **批次十三（2026-08-23 立项，本人拍板口径）完结**：T-038 榜单"隐藏已打标"过滤、T-039 标签分类——均已通过并当场归档（2026-08-23 本人页面验证通过；commit dacda6e＋c1c64a7；均已部署生产，回环全 200；OpenSpec 双 change `board-hide-tagged`/`tag-categories` 归档沉淀 specs）。验收后修复两 bug（c1c64a7）：新建分类按钮误带 tag-add 类被 radar.js 委托先命中（误开打标输入框报 400）、静态资源无版本号被浏览器启发式缓存拿旧文件（新样式/脚本不生效）；治本＝独立 cat-create 类＋base.html 引用带 mtime 版本号。
- **hotfix（2026-08-23，快速模式）**：快照批内坏 node_id 剔除重试——生产 08-21/08-22 连续两天同一批 100 仓因一个已删除仓的 node_id 不可解析整批缺采、每日复炸不自愈；修复＝GitHubError 提取坏 id 标 dead=1 剔除后重试本批，提不出坏 id 才落回"跳过本批"兜底（commit dd767d7，verify 417 绿，k3 评审一轮过零 F1/F2、F3×3 留痕不修）；已部署生产（包 sha256 f6348c86… 两端一致，.env.prod 按新口径覆盖，回环 5 端点全 200）。次日起跑批自愈，坏仓自动标 dead 停采。
- **批次十二（2026-08-20 立项，AI 成本治理，grilling 共识 Q1~Q4）完结**：起因＝DeepSeek 余额烧光事故（本地服务未停全量跑＋每日 AI 段高消耗）。处置留痕：本地服务调度与 AI 开关环境化（AI_ENABLED/RADAR_JOBS_ENABLED，commit ba190c8/5766888）、AI 客户端换 OpenAI SDK（0e85ab4）、403 内容审核单仓失败不再炸整批（b157abb）；本地库已用中转站 AI 补齐（177 次调用、1 仓概要内容审核跳过）。T-037 落地降本四共识（README 截断 3000＋total/概要/季榜重生改每月 1/15 窗口、补缺不受限、周榜维持；OpenSpec change `ai-refresh-cadence` 已归档沉淀 specs）——已通过并当场归档（verify.ps1 413 passed 一次跑绿，k3 评审收口：F2×1 已修、F3×4 带入；commit 46ca6bc；2026-08-20 本人拍板免复验步骤卡）。部署留痕（2026-08-20）：本地库推回生产＋重启＋冒烟——sha256 两端一致（库 03f30a56…、包 8bcea5e8…），openai 3.3.1 生产新装，.env.prod 按新口径从本地覆盖生产（首次执行），回环 6 端点全 200、公网 401 正常。
- 执行顺序：T-035/T-036 已于 2026-08-20 本人页面验证通过并当场归档（批次十一完结）；T-014（部署后 7 天观察，2026-08-14 起算，约 08-21 到期）→ 版本验收 v0.1。
- 批次十一（2026-08-19 立项，grilling 共识 Q1~Q7＋二轮追加拍板）完结：T-035 打标输入自绘下拉（含追加：点开即列既有标签按使用频率降序，§3.3 v2.9）、T-036 智能搜索追问＋20 条＋filters 白名单（创建时间/星数下限＋"暂不支持"回显，OpenSpec change `search-follow-up` 已修订 strict 过并已归档沉淀 specs，§17 v2.9）——均已通过并当场归档（2026-08-20 本人页面验证通过；commit c7b8148＋1bf94ff、9688b02＋571cf8d；均已部署生产，生产真实 POST 冒烟通过）。
- 批次十（2026-08-18 立项，grilling 共识＋OpenSpec 双 change）完结：T-033 榜单三段结构、T-034 智能搜索——均已通过并当场归档（2026-08-19 本人复验；commit 0a32ad2、007d1ef；均已部署生产）。
- 批次九（2026-08-15 立项，词表治理，决策 3 v2＋决策 12）完结：T-031 词表单复数归一＋补 ai-agents——已通过并当场归档（2026-08-18 本人页面过目验收，commit a7f1b35，生产已核 deepseek-harness 进 AI 榜新崛起区）；T-032 每周候选词扫描——已通过并当场归档（2026-08-15 本人复验，commit be23dea，已部署生产）。首批 AI 候选收词落地：137→410 词（收 273、剔 14 个过泛词，commit 3254dfb，已部署生产并重跑预计算）。
- 领取队列：（空）
- 批次八（2026-08-15 立项）完结：T-029 手动同步按钮、T-030 新崛起区前置，均已通过并当场归档（2026-08-15 本人复验；commit ead96a8＋711cf36、a53ef84；均已部署生产）。
- 批次七（2026-08-14 立项）完结：T-027 榜单预计算、T-028 主榜 Top50＋元信息消歧，均已通过并归档（2026-08-14 复验；commit 7ad8d55、caa33f2＋cb05fb9；均已部署生产）。
- T-012/T-013 已通过并归档（2026-08-14 复验；起草产物 commit 981216d）：T-012 生产部署四批次完毕（错密码 401/对密码 200/Let's Encrypt 证书/kill -9 自拉起实测全过）；T-013 备份链路上线（cron 03:17 已装＋真实首备 460K＋恢复演练 916 仓/1441 快照/integrity ok；BACKUP_REMOTE 异机未到位，留痕探测记录下一步第 4 条）。
- 批次五（2026-08-12 立项）：T-018~T-021 等 8 项全部通过并归档（T-018~T-020、T-022~T-025 于 2026-08-13 复验；T-021 于 2026-08-14 复验）。
- T-014 挂部署后 7 天观察（2026-08-14 部署完成起算：首份自动周报产出＋17 榜齐全）；版本验收 v0.1（验收记录.md）挂 T-014 后。
> 已完结任务（T-001~T-039，除 T-014 挂观察外全部）主表行与详情卡已归档至 docs/sop/归档/任务拆解表-已完结.md（最近归档 2026-08-23）。**归档机制（2026-08-12 本人拍板）：任务"已通过"后立即当场归档，不攒批。**

| 任务ID | 目标一句话 | 涉及文件 | 依赖 | 风险级 | 验收标准 | 验证方式 | 状态 | 打包组 | 关联需求 | 详情卡 |
|--------|------------|----------|------|--------|----------|----------|------|--------|----------|--------|
| **批次十四：本地生成通道** ||||||||||
| T-040 | 本地 AI 生成回传：导出待生成任务＋回填接口＋人工行保护（`source='manual'` 不被自动路径覆盖） | `app/local_ai.py`（新）、`app/ai.py`、`app/schema.sql`、`app/db.py`、`app/web/routes.py`、`tests/test_local_ai.py`（新）、`tests/test_ai.py`、`tests/test_db.py`、`docs/sop/交互流程/20-本地AI生成回传.md`、`docs/sop/交互流程/08-推荐语增强.md` | T-037 | 高 | 导出/回填两接口与每日 ensure 判定逐条同口径；默认只补缺、`overwrite=true` 才覆盖；跨期回填拒绝；`source='manual'` 行不被任何自动路径覆盖；统一验证入口一次跑绿 | 机器验证（`tools/verify.ps1`）＋真实库副本闭环预演（A 级）＋生产小批次实跑 | 进行中 | — | 功能 1/4/5 文本来源 | 见下 |
| **批次四：AI 与运维** ||||||||||
| T-014 | 版本级验收（首期 7 天观察+四格尺走查） | `docs/sop/验收记录.md` | T-013 | 低 | 部署后第 7 天第一份周报自动产出、17 榜齐全；对照功能闭环清单逐功能走查 | 本人真实设备走查（AI 自测不算数） | 待领 | — | 全部 | — |
> 状态流转：待领 → 进行中 → 待验收 → 已通过 / 打回（打回须写明原因）
> 风险级判定：高风险＝动数据结构或迁移、改已验收功能或公共组件、删除/权限类操作；低风险＝独立新页面、纯样式调整、文档修改。

## 任务详情卡（高风险/核心链路必填，低风险免）

### 预登记（评审转办，领起对应任务时并入其详情卡）
（历史预登记条目均已落实并入各任务详情卡，随卡归档；2026-08-12 清理。）

### T-040 本地 AI 生成回传（高风险）
- 已确认事实：生产 AI 段按本人要求停用（2026-09-12 `AI_ENABLED=0`，进程生效环境已核）；文本写入现有四条路径（`app/ai.py:658` `recommend_missing`、`app/web/routes.py:2029` `api_recommend`、`app/web/routes.py:2148` `api_recommend_missing`、`app/collector/discover.py:143` 简介变更 DELETE）；`recommendations` 表无来源列（`app/schema.sql:73`）；prompt 构造内嵌于 `DeepSeekClient.translate/recommend/summarize`（`app/ai.py:232/244/322`）；判定与范围复用点 `_scope_sets`（`app/ai.py:570`）、`_ReadmeState`（`app/ai.py:618`）。裁定依据＝OpenSpec change `local-ai-relay`。
- 不改什么：生成节奏与范围（周榜维持每周重生、S 全集不变）、页面模板与渲染、`AI_ENABLED` 状态、每日跑批时序、prompt 文本内容（仅抽取为纯函数，逐字不动）。
- 验证命令及结果：`tools/verify.ps1`＝**523 passed / exit 0**（主 agent 自跑两次；实施者每次交付各跑一次）；`openspec validate --all --strict` 6 项全过；**A 级真实环境预演**（真 uvicorn ＋ 生产库副本 22.5MB、`AI_ENABLED=0`、真 GitHub README）：导出 translate/week（week 任务 user 3207 字符含 README 节选）→ 回填 `written=1` → 重复回填 `skipped=1` → 过期期次/仓库不在池 `failed=2`（errors 文案精确）→ translate 缺 `src`/错 `src` 拒收、对 `src` 写入 → 页面渲染含回填文本与译文 → 启动迁移把 7565 行存量标 `ai`；预演留档 `data/_t040_rehearse2.txt`、`data/_t040_sheet.txt`。
- 独立评审结论：**四轮**（初审 → 修复 → 复审 → 修复 → 三审 → 收口确认），**零 F1**；F2 三条全部修复清零（F2-1 窗口日探测预算被 total 段吃光致 summary 整轮零判定；F2-2' `limit` 挤占 summary 段整轮零跑且零信号，加"只扫名不探测"尾扫并为两条路径补信号；F2-c"收工判据＝`probe_skipped` 为 0"在真实规模不可达〔候选 1413 vs 单请求上限 800〕，改判据为"本批无 total/概要重生任务"、`probe_skipped` 定性为告警值）；F3 已修 10 条（env 覆盖锁、只读回归断言、AI 概要前缀、注释/文案口径等），**留痕不修 3 条**（F3-1 导出同步阻塞事件循环约 8s、F3-6 简介变更连带删除人工行、F3-7 回填 1MB"读完再拒"）均已在 `design.md` 登记边界与理由。
- commit 号：**056e27d**（15 文件、+2978/−96；含 app×5、tests×3、openspec change 四件产物、dev-commands §4.4）
- 复验步骤卡位置：无新增界面元素（纯接口）→ 免页面走查；接口自测按 `docs/dev-commands.md` §4.4（已部署，本人可直接跑；公网形态需 Basic Auth 密码）。
- 部署记录（2026-09-13）：tar+scp 覆盖 `/opt/radar`（包 sha256 `5cafdfeabe3d7731…` 两端一致）＋ `.env.prod` 覆盖（保持 `AI_ENABLED=0`）＋ `systemctl restart radar`；回环 5 端点（`/`、`/?board=all`、`/total`、`/total?board=all`、`/search`）全 200；启动迁移生效（`recommendations` 增 `source` 列，7565 行存量标 `ai`）；**生产小批次真闭环**：`limit=3` 导出 3 条含真 GitHub README（user 3092~3207 字符）→ 零可见变化回填 `written=1` → 过期期次/仓库不存在 `failed=2`（errors 文案精确）→ 落库 `source='manual'` → 首页 200 且目标文本仍在。留档 `data/_t040_smoke.txt`。
- 阻塞原因：【无】

### T-XXX
- 根因/已确认事实：【修复类必填，钉到 文件:行号】
- 不改什么：【边界约束】
- 验证命令及结果：【　】
- 独立评审结论：【findings 及处置或留理由】
- commit 号：【　】
- 复验步骤卡位置：【　】
- 阻塞原因：【如有】

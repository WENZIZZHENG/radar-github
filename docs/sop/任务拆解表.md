# 任务拆解表（阶段⑤冻结物）

> 原子任务硬约束：一次会话能做完、可独立提交、精确到文件路径、自带验收标准。
> 风险级：高风险逐个验收；低风险可打包（每包 ≤3 个、同功能内相邻）。本表打包组：P1={T-008, T-010}（页面与标签，同功能相邻，T-009 高风险夹在中间故不打包，两任务在 T-009 前后顺序执行、单独成组验收）。P2={T-022, T-023}（标签小件，同功能相邻）。其余低风险任务逐个验收。
> 确认状态：【已确认】　确认日期：2026-08-09　版本：v1　基线：dev-sop v1.4

## 当前速览
- 执行顺序：批次四残余（T-012/T-013 → T-014）与批次五残余（T-021）排队——**服务器信息（SSH 访问方式＋域名指向）到位仍优先 T-012/T-013 生产执行**；T-021 原型已确认（2026-08-13）待开工。
- 开工前置：T-021 开工前先按 dev-sop 同步规格类冻结物（功能闭环清单/交互流程说明按需）；P6 分类指向（B 按自定义标签筛选）与左侧边栏视觉分档已拍板（2026-08-13，详见 T-021 行/详情卡），新视觉元素按示意图分档先出原型本人确认再开发。
- 领取队列：T-021 已领起（2026-08-13 原型已确认，待开工）；批次五其余任务均已通过并归档，无排队项。
- T-012/T-013 进行中（起草产物 commit 981216d；**阻塞：待本人提供服务器 SSH 访问方式与域名指向**）。
- 批次五（2026-08-12 立项）：T-018~T-020、T-022~T-025 共 7 项已通过（2026-08-13 本人复验）并归档；T-021 进行中（原型已确认待开工）。
- T-014 挂部署后 7 天观察；版本验收 v0.1（验收记录.md）挂 T-014 后。
> 已完结任务（T-001~T-025，除进行中的 T-021）主表行与详情卡已归档至 docs/sop/归档/任务拆解表-已完结.md（最近归档 2026-08-13）。**归档机制（2026-08-12 本人拍板）：任务"已通过"后立即当场归档，不攒批。**

| 任务ID | 目标一句话 | 涉及文件 | 依赖 | 风险级 | 验收标准 | 验证方式 | 状态 | 打包组 | 关联需求 | 详情卡 |
|--------|------------|----------|------|--------|----------|----------|------|--------|----------|--------|
| **批次四：AI 与运维** ||||||||||
| T-012 | 部署与鉴权（含服务器现状探测） | `deploy/Caddyfile`, `deploy/radar.service`, `docs/sop/服务器探测记录.md` | T-011 | 高 | 端口/已有服务探测留痕；HTTPS 证书自动签发；错误密码 401；systemd 重启自拉起 | 服务器真实部署验证 + 错密码实测 | 进行中（起草完成待服务器信息） | 981216d | 全部 | 必填 |
| T-013 | 备份脚本与定时 | `scripts/backup.sh` + 服务器 cron | T-012 | 低 | 每日 .backup+gzip 轮转 14 份；每周 scp 异机；**恢复演练**：恢复到临时库并能查询 | 真实执行备份+恢复演练留痕 | 进行中（起草完成待服务器信息） | 981216d | 全部 | — |
| T-014 | 版本级验收（首期 7 天观察+四格尺走查） | `docs/sop/验收记录.md` | T-013 | 低 | 部署后第 7 天第一份周报自动产出、17 榜齐全；对照功能闭环清单逐功能走查 | 本人真实设备走查（AI 自测不算数） | 待领 | — | 全部 | — |
| **批次五：榜单增强与想法池激活（2026-08-12 立项）** ||||||||||
| T-021 | 左侧可隐藏导航栏＋我的关注页分类（两想法合并实施） | `app/web/templates/base.html`＋各页模板, `app/web/static/radar.css`, `app/web/static/radar.js`, `app/web/routes.py`, `tests/*` | T-018 | 高 | 2026-08-13 本人拍板：①P6 分类=B 按自定义标签筛选（A 撞 v1.3 否决理由、C 被边栏吸收；流程说明 §12.2）；②左侧边栏仅 P1/P2/P3，sticky 常驻可收起状态本地记住，移动端默认收起抽屉式，原顶部锚点 chips 区由边栏替代（吸收主题换行优化点；§12.1）；新视觉元素先出原型本人确认再开发；既有页面内容与功能零回归 | 浏览器走查＋真实页面截图 | 进行中（原型已确认 2026-08-13，待开工） | — | S1, S2 | 必填 |
> 状态流转：待领 → 进行中 → 待验收 → 已通过 / 打回（打回须写明原因）
> 风险级判定：高风险＝动数据结构或迁移、改已验收功能或公共组件、删除/权限类操作；低风险＝独立新页面、纯样式调整、文档修改。

## 任务详情卡（高风险/核心链路必填，低风险免）

### 预登记（评审转办，领起对应任务时并入其详情卡）
（历史预登记条目均已落实并入各任务详情卡，随卡归档；2026-08-12 清理。）

### T-012
- 根因/已确认事实：新功能（高风险：上生产＋权限/鉴权类）。规格钉死：《架构决策记录》决策 8（Caddy + Basic Auth，无应用内用户系统；写操作与 AI 触发必须挡外人；Caddy 自动 HTTPS）；本人已拍板有域名（2026-08-09）。部署形态事实：FastAPI 单进程 uvicorn（`uvicorn app.main:app --port 8000`），APScheduler 进程内随 lifespan 启动（RADAR_JOBS_ENABLED 默认开，生产不置 0）；SQLite data/radar.db（WAL）；.env 项目根（GITHUB_TOKEN/DEEPSEEK_API_KEY）；Python ≥3.12＋uv。**密钥与敏感信息不入库（S 档发布就绪）**：Basic Auth bcrypt hash、域名一律环境变量占位（Caddy `{$VAR}` 支持），真实值只进服务器环境。**今晚（无人值守批次）只起草不执行**：deploy/Caddyfile、deploy/radar.service、scripts/backup.sh（T-013 一并）、docs/sop/服务器探测记录.md（待执行命令清单＋结果留白）；**阻塞：无服务器 SSH 访问方式与域名指向信息**，真实部署/探测/错密码实测/备份恢复演练待本人提供后由主 agent 上生产执行（任务表速览已登记）。
- 不改什么：app/ 与 tests/ 现有代码一行不动；不引新依赖；不在仓库写入任何真实密钥/hash/域名。
- 验证命令及结果：起草阶段——① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` 一次跑绿 **153 passed**（2026-08-10；跑前揪出并修复 test_web.py /total 推荐语种子本地/UTC 周标签错位的跨日必红，单独 commit f57d464）；② `bash -n scripts/backup.sh` 通过；③ 备份轮转管线真实执行（20 假文件→保留恰好 14 份最新，主 agent 亲测）。生产验证（探测/证书/401/自拉起/备份恢复演练）待服务器信息。
- 独立评审结论：explore 评审 agent（primary，agent-23）**0 高 1 中 4 低，修复后放行**——中-1 radar.service 部署指引死路（nologin shell 与 `sudo -iu radar` 冲突＋system user 无家目录，已修：useradd 加 -m＋步骤 3 改 `sudo -u radar -H bash -c` 显式形态；评审静态推断、明日上生产最先验证该步）；低-1 `basic_auth` 改名版本事实错误（已修：v2.6→v2.8.0，评审联网核实 release notes/issue #6418）；低-2 backup.sh 周日分支 head SIGPIPE 理论误伤面（已修：改 sed -n '1p'）；低-3 恢复演练无书面步骤（已修：backup.sh 头注释补 3 行）；低-4 异机免密未覆盖（已修：探测记录下一步补第 4 条）。原评审 agent 复审确认 5 条全清零、delta 无新问题。
- commit 号：981216d（起草产物 4 文件）；f57d464（test_web UTC 修复）
- 复验步骤卡位置：起草物本身即明日执行手册——探测按 `docs/sop/服务器探测记录.md` 11 项逐条执行回填；部署按 `deploy/Caddyfile`/`deploy/radar.service` 头注释；备份按 `scripts/backup.sh` 头注释（含 cron 行与恢复演练步骤）。
- 阻塞原因：服务器 SSH 访问方式与域名指向信息未提供（待本人）；真实部署/探测/错密码 401 实测/备份恢复演练均为此后的服务器执行项。

### T-021
- 根因/已确认事实：变更（高风险：改公共组件 base.html 与已验收页面）。来源：想法池 2026-08-09 左侧可隐藏导航栏（本人 T-008 走查提出）＋我的关注页 P6 分类（本人 T-015 走查提出并拍板合并实施），2026-08-12 过表纳入。规格：锚点 chips/筛选导航挪到左侧边栏并可收起，榜单长页滚动筛选时不用回到顶部。**已拍板（2026-08-13）**：①P6 分类=B 按自定义标签筛选（A 撞 v1.3 否决理由、C 被边栏吸收；流程说明 §12.2）；②左侧边栏仅 P1/P2/P3，sticky 常驻可收起状态本地记住，移动端默认收起抽屉式，原顶部锚点 chips 区由边栏替代并吸收主题换行优化点，边栏与榜头均不设"回顶部"（§12.1）；原型 docs/sop/原型-T021.html 已经本人确认（2026-08-13）。
- 不改什么：页面内容区结构、榜单行形态、功能逻辑零回归；视觉体系（radar.css 设计 token）沿用；不引新依赖。
- 验证命令及结果：【待开工填】
- 独立评审结论：【待开工填】
- commit 号：【待开工填】
- 复验步骤卡位置：【待开工填】
- 阻塞原因：无

### T-XXX
- 根因/已确认事实：【修复类必填，钉到 文件:行号】
- 不改什么：【边界约束】
- 验证命令及结果：【　】
- 独立评审结论：【findings 及处置或留理由】
- commit 号：【　】
- 复验步骤卡位置：【　】
- 阻塞原因：【如有】

# 任务拆解表·已完结（归档）

> 归档说明：来源文件 `docs/sop/任务拆解表.md`；归档日期 2026-08-11（最近 2026-08-13）；归档规则（2026-08-12 本人拍板）：任务"已通过"后立即当场归档——其主表行、详情卡与速览完成句逐字搬入本文件，活文件只留未完成任务与现行口径。

## 主表行（已通过任务，逐字搬自 `docs/sop/任务拆解表.md`，最近归档 2026-08-13）

| 任务ID | 目标一句话 | 涉及文件 | 依赖 | 风险级 | 验收标准 | 验证方式 | 状态 | 打包组 | 关联需求 | 详情卡 |
|--------|------------|----------|------|--------|----------|----------|------|--------|----------|--------|
| **批次一：数据底座** ||||||||||
| T-001 | 项目骨架与统一验证入口 | `pyproject.toml`, `app/config.py`, `app/main.py`, `tests/test_smoke.py`, `tools/verify.ps1` | 无 | 低 | uvicorn 起服务首页返回 200；`tools/verify.ps1` 一次跑绿（lint+pytest） | 真实起服务访问；verify 脚本执行 | 已通过 | — | 全部 | — |
| T-002 | SQLite schema 与连接层 | `app/schema.sql`, `app/db.py` | T-001 | 高 | 建表幂等（重复执行不炸）；WAL 开启；五个表+索引齐全 | pytest 对真实 sqlite 文件执行建表与读写 | 已通过 | — | 全部 | 必填 |
| T-003 | GitHub 客户端（GraphQL 批量+Search+限速重试） | `app/collector/github.py`, `tests/test_github.py` | T-002 | 高 | `nodes(ids:)` 一次拉 100 仓库 stargazerCount 成功；Search 限速 ≤30/min；token 失效报错清晰 | **真实 API 执行**拉取 100 个知名仓库留痕（协议类必须真实执行）；限速逻辑单测 | 已通过 | — | S1 | 必填 |
| T-004 | 分类器与主题词表 | `app/classify.py`, `config/topics.yaml`, `tests/test_classify.py` | T-002 | 低 | 命中/多命中/零命中/language null 边界全有单测；词表本人封板 | pytest | 已通过 | — | S1 | — |
| **批次二：采集与榜单** ||||||||||
| T-005 | 初始快照任务（核心池分片枚举+批量入库） | `app/collector/snapshot.py`, `tests/test_snapshot.py` | T-003, T-004 | 高 | 分片逻辑不越过 1000 条上限；真实小规模执行（1 个分片）入库正确；断点续跑 | 分片单测 + 真实执行 1 分片留痕 | 已通过 | — | S4 | 必填 |
| T-006 | 每日快照+发现池+调度注册 | `app/collector/discover.py`, `app/jobs.py`, `tests/test_jobs.py` | T-005 | 高 | 每日 UTC 00:00 快照+发现查询注册成功；连续失败 3 天静默重试不报警；任务日志可查 | dry-run 真实执行一次全日任务；静默重试单测 | 已通过 | 28b4498 | S1 | 必填 |
| T-007 | 榜单计算 SQL（周/季/总星三口径） | `app/report.py`, `tests/test_report.py` | T-006 | 高 | 滑动窗口/首周缺席/负增量沉底/dead 剔除四种口径单测全覆盖；Top N 不足按实际 | 单测 + 对真实库执行一次完整榜单 SQL（SQL 类必须真实执行） | 已通过 | 4fdce5e | S1, S3 | 必填 |
| **批次三：页面与交互** ||||||||||
| T-008 | 榜单页面（首页周报/历史周次/季榜/总星榜） | `app/web/routes.py`, `app/web/templates/*.html`, `app/web/static/*` | T-007 | 低 | 17 榜渲染与示意图一致；历史周次可切换；空态（首期无周报时显示总星榜）正常 | 浏览器自动化走查真实服务 | 已通过 | bb83d89 | S1, S3, S4 | 有 |
| T-009 | 关注功能（含动态入池） | `app/web/routes.py`(关注 API), `app/collector/snapshot.py`(单仓库基线), `app/web/templates/*` | T-008 | 高 | 关注未入池仓库→立即抓基线并入每日跟踪；刷新后关注仍在；取消关注保留跟踪；页顶"我的关注"显示当周增量 | 浏览器走查 + 库内状态核对 | 已通过 | 92069aa | S2 | 必填 |
| T-010 | 标签功能（增删+筛选） | `app/web/routes.py`(标签 API), `app/web/templates/*`, `app/web/static/radar.{js,css}`, `tests/test_tags.py` | T-008 | 低 | 标签增删、按标签筛选、刷新后仍在 | 浏览器走查 | 已通过 | eba66e6 | S2 | — |
| **T-015**（批次三追加） | 关注独立页 P6（D4 翻案：页顶关注区→独立页＋导航入口＋语言分组） | `app/web/routes.py`, `app/web/templates/*`, `app/web/static/radar.js`, `app/web/static/radar.css`, `tests/test_web.py`, `tests/test_follows.py` | T-009 | 高 | `/follows` 按语言 7 组展示（组内当周增量降序、组间按组内最大增量降序、空组不渲染）；行形态同榜单行（紧凑行＋默认展开＋★）；三态沉组尾（"—— 下周起有数据"/dead 灰显"已失效"）；顶栏 5 项"我的关注"带计数徽标、各页 SSR 一致；P1 顶部关注区移除；关注/取消后计数前端局部 +1/-1；P6 内取消该行即时移除；空关注空态引导 | 浏览器走查＋真实页面截图＋关注 API 回归 | 已通过 | 133794f | S2 | 必填 |
| **批次四：AI 与运维** ||||||||||
| T-011 | AI 服务（懒翻译+推荐理由+降级） | `app/ai.py`, `app/jobs.py`, `tests/test_ai.py` | T-008 | 高 | 中文描述跳过；上榜无翻译项目批量补译；推荐理由按项目生成跨榜复用；AI 失败时榜单照常出（英文原文/无推荐语） | **真实调用 DeepSeek** 一次留痕；降级路径单测 | 已通过 | 94556b2 | S1 | 必填 |
| **T-016**（批次四追加） | 翻译增强（全池覆盖＋原文变更自动重译＋手动单/批量触发） | `app/collector/discover.py`（每日比对 description_en）, `app/ai.py`（翻译段扩全池）, `app/web/routes.py`+`app/web/static/radar.js`+`app/web/templates/_row.html`（手动触发 API+按钮）, `tests/*` | T-011 | 高 | 全池未译（含 dead）每日批量补译；description_en 变更即更新原文＋清旧译文当日重译；行内单个强制重译即时反馈；批量补译防重入；AI 失败降级照旧（不阻塞榜单） | 浏览器走查＋真实库核对＋降级单测 | 已通过（本人复验步骤 1/5 通过 2026-08-11，commit 2275a14） | 2275a14 | S1 | 必填 |
| **T-017**（批次四追加） | 推荐语扩维＋翻译收窄（推荐语三榜∪关注集＋分维度文本＋行内/页头生成按钮；翻译收窄上榜∪关注） | `app/ai.py`（ensure 三榜集＋分维度生成）, `app/schema.sql`+`app/db.py`（recommendations 加 dimension 列幂等迁移）, `app/web/routes.py`+`app/web/static/radar.js`+`app/web/templates/_row.html`+`base.html`（推荐语单个/批量 API+按钮）, `tests/*` | T-016 收口 | 高 | 每日 ensure 补齐三榜∪关注集缺译与缺推荐语；周/季文本每周重生成、总星文本懒口径（缺时生成＋简介变更重生）；行内按钮按当前页维度单个强制重生（toast＋面板局部替换不刷新）；页头批量补齐后台任务进度同翻译；翻译 ensure/批量收窄上榜∪关注（手动单个不受限，已译保留）；schema 迁移幂等 0 行无损；AI 失败降级照旧不阻塞出榜 | 浏览器走查＋真实库核对＋降级单测 | 已通过 | — | S1 | 必填 |
| **批次五：榜单增强与想法池激活（2026-08-12 立项）** ||||||||||
| T-018 | 周/季榜新崛起区（决策 4 v2：未满窗仓分区展示在池增量） | `app/report.py`, `app/web/routes.py`, `app/web/templates/*`, `app/ai.py`, `tests/*` | T-017 | 高 | 周/季各分类榜设新崛起区：主榜出席校验失败（在池跨度不足最小窗口）仓按在池增量（端点−入池基线快照）降序 Top 10 分区展示、逐行标注"入池 N 天增 X"、不与主榜混排；跨度满窗自动毕业；新区行纳入翻译/推荐语上榜口径（推荐语用对应维度、prompt 传入池语境）；准入/排序/毕业/空区单测 | 单测＋浏览器走查真实服务 | 已通过（2026-08-13 复验） | — | S1, S3 | 必填 |
| T-019 | 发现池补捞机制（每周补捞"新涨 ≥1000 星但近期无更新活动"盲区仓） | `app/collector/discover.py`, `app/jobs.py`, `tests/*` | T-018 | 高 | 每周一次补捞查询（created 排序或星数区间轮换，方案实施时定并留痕）注册进调度；补捞入池即抓基线快照；不破 Search 单查询 1000 条上限与配额；单测＋dry-run 真实执行一次留痕 | 单测＋真实执行留痕 | 已通过（2026-08-13 复验） | e34583c | S1 | 必填 |
| T-020 | 榜单端点新鲜度（防长期漏更未标 dead 仓以旧窗口混榜） | `app/web/routes.py`＋模板（行级展示端点日期） | T-018 | 低 | 方案 A 已拍板（2026-08-12）：行级详情面板可见端点快照日期（个别仓陈旧一眼可辨）；B（计算 freshness 约束）已否，A 不够用再议 | 单测＋浏览器走查 | 已通过（2026-08-13 复验） | 14ad72d | S1, S3 | — |
| T-022 | 打标输入框已有标签下拉建议（原生 datalist） | 标签输入所在模板, `app/web/routes.py`（注入既有标签列表）, `tests/*` | — | 低 | 打标输入框 datalist 自动补全既有标签（防"AI"/"ai"/"人工智能"发散致筛选失效）；自由输入不受限；既有标签全量一次注入（几十个量级）；零新组件零依赖 | 浏览器走查 | 已通过（2026-08-13 复验） | 0232b1c | S2 | — |
| T-023 | 标签删除防误删（toast 带撤销按钮，B 方案） | `app/web/static/radar.js` | — | 低 | 点 × 删标签后 toast 带"撤销"按钮，数秒内可撤销恢复；现有增删 API 幂等直接支持，不改服务端；撤销与超时两路径浏览器走查 | 浏览器走查 | 已通过（2026-08-13 复验） | 0232b1c | S2 | — |
| T-024 | AI 深度摘要（详情面板 AI 概要） | `app/ai.py`, `app/web/routes.py`, `app/web/templates/*`, `tests/*` | T-018 | 高 | 形态已拍板（2026-08-12）：详情面板 AI 概要（段落级"是什么"）、与推荐语并存不替代；覆盖上榜∪关注同 T-017 口径；README 抓取链路复用 T-017；成本 ¥ 级/周；AI 失败降级不阻塞出榜 | 单测＋真实 DeepSeek 一次留痕＋浏览器走查 | 已通过（2026-08-13 复验） | be45f5e | S1 | 必填 |
| T-025 | language/topics 漂移检测（每日比对刷新分类归属） | `app/collector/github.py`（nodes 查询加字段）, `app/collector/discover.py`（比对更新）, `tests/*` | T-019 | 高 | nodes 批量查询加 primaryLanguage/topics 字段；每日比对、变更即 UPDATE repos（topics JSON 序列化口径同既有写入方）；变更对推荐语输入的联动口径实施时定并留痕；单测＋真实执行留痕 | 单测＋真实执行留痕 | 已通过（2026-08-13 复验） | ba8638e | S1, S3 | 必填 |

## 任务详情卡（已通过任务，逐字搬自 docs/sop/任务拆解表.md）

### T-002
- 根因/已确认事实：新功能（数据底座）；数据模型以《架构决策记录》§2 为准（五表＋WAL＋榜单实时 SQL 计算，不物化）。
- 不改什么：不动任何既有文件（config.py 的 .env 加载归 T-003）；不引新依赖（标准库 sqlite3）；字段类型与索引细节授权实施决定，索引须服务榜单查询（按日期取各仓库最近快照、两端差值排序）。
- 验证命令及结果：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest 6 passed（2026-08-09，commit 前最后一跑）。
- 独立评审结论：explore 评审 agent（primary）5 条 findings——①外键生效无测试锁定（中）、②榜单端点 SQL 形态无载体（中低）、③时间格式无硬约定（低）、④busy_timeout 隐式依赖（低）、⑤init_db 注释超前（低）。全部修复（补外键生效＋数据保留两个测试、schema 注释补两处硬约定、timeout=5 显式化、注释措辞修正），原评审 agent 复审确认 5 条全清零。②③⑤ 的遗留动作转办后续任务（见下方预登记）。
- commit 号：216c39b
- 复验步骤卡：步骤 1——终端在 `D:\project\radar-github-new` 执行 `powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1`，预期末尾 `All checks passed!` ＋ `6 passed`；步骤 2（可选看实物）——`type app\schema.sql` 看五表定义。步骤 1 绿即通过；预估 1 分钟。
- 阻塞原因：无

### T-003
- 根因/已确认事实：新功能；数据源以《架构决策记录》§3 决策 1 为准（GraphQL `nodes(ids:)` 批量为主，Search 仅发现/初始分片）；.env 已配 GITHUB_TOKEN（无 scope 经典 PAT，仅提配额，本人确认 2026-08-09）。
- 不改什么：不改 schema.sql/db.py/main.py；config.py 仅加 .env 加载（系统环境变量优先，不引 python-dotenv）；不引新依赖（httpx 已在主栈）；禁止读取/打印 token 与 .env 内容；产生时间戳处遵守 UTC 定长硬约定（上方预登记并入）。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest 29 passed（2026-08-09，commit 前最后一跑）；② 真实 API 自检 `.venv/Scripts/python.exe -m app.collector.github` → 2026-08-09T04:53:24Z 执行，100/100 仓库 stargazerCount 全部获取，留痕 `data/t003_selftest.json`（不入仓）。
- 独立评审结论：explore 评审 agent（primary）5 条 findings——中-1 .env BOM/GBK 编码坑、中-2 UTC 定长时间戳无测试锁定、低-1 非限速 403 白等 5 分钟、低-2 GraphQL partial errors 整体丢弃、低-3 Search 1000 条上限无提示。处置：中-1/中-2/低-1/低-3 修复并各带测试锁定；**低-2 留理由不修**：nodes 批量查询 partial errors 极罕见，S 档单人工具整体报错重跑可接受。原评审 agent 复审确认 4 条清零＋低-2 不修成立。
- commit 号：dc6c1c8
- 复验步骤卡：步骤 1——终端在 `D:\project\radar-github-new` 执行 `powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1`，预期末尾 `All checks passed!` ＋ `29 passed`；步骤 2（可选，真实打 GitHub API，约耗 2 次请求配额）——`.venv\Scripts\python.exe -m app.collector.github`，预期输出"自检通过：100 个仓库 stargazerCount 全部获取"及前 5 个仓库名与星数。步骤 1 绿即通过；预估 1 分钟（含步骤 2 约 2 分钟）。
- 阻塞原因：无

### T-005
- 根因/已确认事实：新功能；核心池 ≥1000 星约 6.4 万（共识文档＋架构决策决策 2 实测）；GitHub Search 单查询只吐前 1000 条（T-003 客户端 docstring 已提示）；REST search item 自带 full_name/description/language/topics/stargazers_count/node_id，初始枚举一次拿全无需 GraphQL（决策 1：Search 仅发现/初始分片）；schema：repos.full_name UNIQUE、star_snapshots 主键 (repo_id, captured_at)；UTC 定长硬约定（预登记并入）；`utc_now_iso()` 在 app.collector.github 已有。
- 不改什么：不改既有文件（github.py/schema.sql/db.py/classify.py 等）；不引新依赖；分片策略与断点格式授权实施决定，但分片逻辑必须保证单查询不越 1000 上限、断点续跑幂等（full_name 冲突跳过）；禁止读取/打印 token。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest 57 passed（2026-08-09，commit 前最后一跑）；② 真实执行 1 叶子分片：`.venv/Scripts/python.exe -m app.collector.snapshot --max-shards 1` → `stars:269450..537898` total_count=13，repos +13／snapshots +13 入 data/radar.db；二次执行断点跳过、行数不变（13/13）；库内核对 source='initial'、topics 为 JSON 文本、distinct captured_at=1。
- 独立评审结论：explore 评审 agent（primary）4 条低 findings，无高/中——低-1 分片间漂移无告警（已修：超阈值 `[告警]` 行＋双向测试）、低-2 根分片上界为探针时刻快照无说明（已修 docstring）、低-3 断点与库发散静默跳过（**留理由不修**：S 档单人，删断点文件即全量重跑且入库幂等，ProgressStore docstring 已写明处置方式）、低-4 多页累积／根探针缺 items 测试缺口（已修＋3 用例）。原评审 agent 复审确认 4 条全清零。**评审盲点留痕**：node_id 未入库的缺口两轮评审均未发现，根因在架构决策 §2 字段清单遗漏，由 T-006 修复（见 T-006 详情卡）。
- commit 号：767dc3d
- 复验步骤卡：步骤 1——`powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1`，预期 `All checks passed!` ＋ `57 passed`；步骤 2（可选）——`.venv\Scripts\python.exe -m app.collector.snapshot --max-shards 1`，预期断点已完成跳过、库内行数不变。本任务验收标准全部机器可执行／已留痕核对，按 2026-08-09 验收口径由 AI 直接验收通过。
- 阻塞原因：无

### T-006
- 根因/已确认事实：新功能＋上游缺口修复。**缺口根因**：《架构决策记录》§2 repos 字段清单漏 `node_id`（决策 1 的 GraphQL `nodes(ids:)` 每日批量采集必需 Relay 全局 ID），致 T-002 建表、T-005 入库连环遗漏且两轮评审未盖住；2026-08-09 按勘误通道修正 §2 并向本人报备。修复面：schema.sql repos 加 `node_id TEXT NOT NULL UNIQUE`；snapshot.py 入库补存；对应测试同步；现库仅 13 行验证数据，删 data/radar.db 重建并重跑 1 分片核对。每日任务以《架构决策记录》决策 1/2/4 为准：核心池每日 GraphQL nodes 批量刷新（约 640 请求）；nodes 返回 null/isPrivate/isDisabled → dead=1 保留历史快照；发现池每日 Search 捞新面孔入池（source='discover'）并抓基线快照；新入池首周缺席口径（决策 4）。静默容错：任务级失败记日志不报警、次日再跑（共识 §8：连续 3 天失败允许数据空洞）。预登记并入：应用启动接线 init_db()（T-002 评审 finding 5 转办）；UTC 定长硬约定。
- 不改什么：除"修复面"（schema.sql 加列、snapshot.py 补 node_id、对应测试同步、删验证库重建重跑 1 分片）外不改既有逻辑；main.py 仅加 startup 接线（init_db＋调度启动，带 RADAR_JOBS_ENABLED 开关默认开，本地开发可关）；不引新依赖（APScheduler 已在主栈）；禁止读取/打印 token。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest 69 passed（2026-08-09，commit 前最后一跑）；② dry-run 真实执行全日任务：`.venv/Scripts/python.exe -m app.collector.discover` → 核心池 13 行快照刷新＋发现查询新捞 499 入池（repos=512，node_id 空=0，source='discover' 基线快照已落）；`data/jobs.log` 汇总行正常；③ `RADAR_JOBS_ENABLED=0` 起 uvicorn 首页 200（启动接线不炸）。
- 独立评审结论：explore 评审 agent（primary）6 条 findings（2 中 4 低）——中-1 改名仓库按 full_name 判重落空致 KeyError 整批回滚（已修：full_name＋node_id 双键判重＋反查判空＋测试）、中-2 GitHubAuthError 被逐批重试白等（已修：认证错误直通＋测试）、低-1 isArchived 误判死库（已修：口径注释——归档仍可贡献不算死）、低-2 发现池只捞 ≥1000 星存在新库盲区（**不修，已入《想法池》**：与共识"不追突发黑马"一致，版本后评审）、低-3 补 3 用例（已修）、低-4 jobs_enabled 开关读取未走 get_settings()（已修）。原评审 agent 复审确认 6 条全清零。**遗留留痕（不修）**：snapshot.py 初始枚举侧同构反查落空风险——与 T-006 同型的 node_id 反查在初始枚举路径也可能落空，保持 fail-loud 既定姿态，遇异常删断点重跑即可（S 档单人，入库幂等）。
- commit 号：28b4498
- 复验步骤卡：步骤 1——`powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1`，预期 `All checks passed!` ＋ `69 passed`；步骤 2（可选，真实执行，耗少量 API 配额）——`.venv\Scripts\python.exe -m app.collector.discover`，预期核心池快照 512 行左右、新发现≈0（已入池的不重复）、`data\jobs.log` 新增一条汇总行。步骤 1 绿即通过；预估 1 分钟。本任务验收标准全部机器可执行／已留痕核对，按 2026-08-09 验收口径由 AI 直接验收通过。
- 阻塞原因：无

### T-007
- 根因/已确认事实：新功能。口径以《架构决策记录》为准——决策 4：周增量＝两端快照差；端点缺失滑动窗口（5~9 天）并标注；新入池首周缺席；负增量沉底；dead 剔除。决策 3：分类全部本地 SQL 计算——语言 7 榜（Java/Go/Rust/TypeScript/JavaScript/Python＋其它）、主题 10 榜（config/topics.yaml 9 主题＋其他，零命中进其他），每榜 Top 30（不足按实际，允许跨榜重复）。季榜＝90 天两端快照差同口径；总星榜＝最新快照降序（不参与增量口径）。预登记硬约束（任务表预登记区，领起并入）：端点 SQL 必须从 repos 驱动 per-repo `ORDER BY captured_at DESC LIMIT 1` 索引 seek，禁止对 star_snapshots 整体 GROUP BY／相关子查询（T-002 评审 EXPLAIN 实测 2300 万行级全扫）。schema：repos(id, full_name, node_id, description_en/zh, language, topics JSON, dead, source, created_at)、star_snapshots(repo_id, captured_at, stars) 主键 (repo_id, captured_at)。classify.py 既有接口：LANGUAGES（6 语言精确名→小写 key）、OTHER_LANGUAGE_KEY="other"、load_topics(path)→dict[str,TopicSpec]、classify_language(language|None)→str、classify_topics(topics, table)→list[str]（空列表＝"其他"榜）。真实库现状：512 alive repos、525 snapshots、仅 2 个 captured_at（同一天 05:51/05:57）——真实执行只验证"SQL 跑得通＋代码路径覆盖"，口径语义靠单测构造数据（窗口 5~9 天、首周缺席、负增量、dead 等场景单测造数）。
- 不改什么：不改采集层任何文件；不调 GitHub/DeepSeek API（纯本地 SQL＋词表）；不改 schema；不改 classify.py 既有接口；不引新依赖；榜单模块 API 形态（函数签名/返回结构）授权实施决定，但必须让 T-008 页面层拿得到 17 榜各自 Top 30 行（含 full_name、description、language、总星、增量、窗口标注）。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest 85 passed（2026-08-09，commit 前最后一跑；69 基线＋16 本任务）；② 真实库执行 `.venv/Scripts/python.exe -m app.report` → 三口径全 17 榜跑通；EXPLAIN QUERY PLAN 两条端点 SQL 均 `SEARCH star_snapshots USING INDEX sqlite_autoindex_star_snapshots_1` 索引 seek、无 SCAN；week/quarter 512 仓库全缺席（库内快照仅同日两个 captured_at，首周缺席预期行为）；total 512 出席、17 榜全部产出、降序正确、Top N 不足按实际（Java Top15、devops Top9、game Top8、automation Top13、security Top20 等）。
- 独立评审结论：explore 评审 agent（primary）5 条低 findings，无高/中——低-1 端点快照无新鲜度约束（**留痕不修**：决策 4 只约束两端跨度 5~9 天，未约束端点距 as_of 远近；长期漏更又未标 dead 的 alive 仓库可能以旧窗口混入当周榜，属冻结物口径盲区而非实现偏差；主触发路径（删除/私有化/改名）已被 T-006 dead 标记盖住，残余风险小；window_days 标注＋页面展示可感知，是否加约束由本人拍板）、低-2 测试四条小缝（已修＋4 用例：零快照/季榜宽侧/增量口径未来快照/次序键）、低-3 topics JSON 无兜底（已修：fail-loud 注释留痕，与 _parse_iso 同姿态）、低-4 季榜 86~94 为实施自定参数（已修：代码注释写明＋本卡留痕——冻结物只钉"季榜 90 天同口径"，86~94＝90±4，容差随窗口量级放宽）、低-5 SQL 红线无自动化锁定（已修：EXPLAIN 断言测试锁两条端点 SQL 常量）。原评审 agent 复审确认 5 条全清零。**遗留留痕**：低-1 端点新鲜度约束不实施（理由如上），是否入想法池由本人拍板。
- commit 号：4fdce5e
- 复验步骤卡：步骤 1——`powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1`，预期 `All checks passed!` ＋ `85 passed`；步骤 2（可选）——`.venv\Scripts\python.exe -m app.report`，预期 EXPLAIN 两行 SEARCH 无 SCAN，week/quarter 全缺席（Top0，首周缺席预期），total 各榜有行（Python Top30 等）。步骤 1 绿即通过；预估 1 分钟。本任务验收标准全部机器可执行／已留痕核对，按 2026-08-09 验收口径由 AI 直接验收通过。
- 阻塞原因：无

### T-008
- 根因/已确认事实：新功能（低风险；有评审挂起项故建卡留痕）。依据：示意图 v3＋交互流程说明 v1.1（均已冻结）；榜单口径全部委托 app.report 不重算；边界钉死：关注写操作 T-009、标签增删 T-010、推荐理由生成 T-011 一律不实现，follows/recommendations/tags 三表只读。
- 不改什么：不改 report/classify/db/collector 任何文件；不引新依赖；main.py 仅去占位首页＋挂 /static＋include_router。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest 93 passed（2026-08-09，85 基线＋8）；② 真实服务（RADAR_JOBS_ENABLED=0，:8011）：/、/?week=2026-W32、/quarter、/total 全 200，/?week=2026-W99 → 400（fail-loud）；首期空态降级提示"首份周报预计将于 8月16日 生成（需满 7 天统计窗口），当前为初始总星榜"；`class="board"` ×17；③ Chrome headless 1440px 截图自审：与 v3 基线一致（五层结构/配色/chips 徽标/默认全部展开）。
- 独立评审结论：explore 评审 agent（primary）2 中 4 低＋3 观察项——中-1 测试时间炸弹 8 天后必红（已修：裸 / 锁 notice class、倒计时字面断言移固定历史周）、中-2 关注区增量全池复算（**转办 T-009 预登记留理由**：目标规模才显现，T-009 正改这块）、低-1 dead 关注卡文案矛盾（已修："已停止采集"）、低-4 星标禁用事件冒泡（已修：`.star` guard，Chrome M120 行为变更）。**挂起待本人拍板 2 条（复审确认路径成立、本体未清零）**：低-2 实现 URL 形态（/?week=、/quarter、/total）与流程说明 §1（/w/<周次>、/q/<季度>、/top）不一致＋P3 分段控件取代"期次下拉"——两个独立拍板点，注意 v3 示意图只覆盖了控件形态、未覆盖 URL 形态；低-3 窗口实际天数标注实现在行级面板（流程说明字面为元信息行）——行级更精确（滑动窗口是逐仓口径），建议勘误改文档适配实现。原评审 agent 复审：中-1/中-2/低-1/低-4 实质清零，低-2/低-3 挂起待拍板——**2026-08-09 本人走查通过并三项拍板全部同意勘误（URL 形态/P3 分段控件/窗口行级标注），流程说明 v1.2 已回改，挂起项清零**。
- commit 号：bb83d89
- 复验步骤卡：随交付消息给本人（页面走查属保留验收项）。
- 阻塞原因：无（2026-08-09 本人页面走查通过＋三项勘误拍板，验收完成）。

### T-009
- 根因/已确认事实：新功能＋T-008 评审中-2 转办（预登记：关注区增量不得对全跟踪池复算）。口径以《架构决策记录》决策 9 为准：关注＝动态入池——关注即抓基线快照并加入每日跟踪（否则低星/未入池项目永远没有"当周增量"）；取消关注保留跟踪（只删 follows 行，不动 repos/snapshots）。采集层事实：`_snapshot_all` 选 `dead=0` 全量仓库每日刷新（discover.py:92）——入池即自动进入每日跟踪，无需改调度。未入池关注三态（钉死）：① 已入池 alive：只插 follows 行；② 已入池 dead：复活（dead=0＋新基线快照）＋follows；③ 未入池：GitHub 抓单仓库元数据→插 repos(source='follow')＋基线快照＋follows。schema：follows(repo_id, created_at)；repos.source 已有 'initial'/'discover' 先例。T-008 接线点：星标按钮 disabled 待启用、toast CSS 样式已在 radar.css、关注区卡片与"—— 下周起有数据"/"已停止采集"口径已渲染。中-2 转办：`compute_repo_deltas` 加 repo_ids 过滤参数（默认 None＝全池，行为不变），`_follow_cards` 只算关注集（report.py 计算逻辑本身一行不许动）。
- 不改什么：不改榜单口径与 report.py 既有计算逻辑（只加可选过滤参数）；不改 schema；不改 discover.py 每日任务主流程；不引新依赖；API 形态（路由/参数/响应）授权实施决定，但必须是 SSR fetch 可用的 JSON 端点＋操作后用户即时感知（toast＋星标态＋关注区更新，可局部或 reload，实施定）。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest 114 passed（2026-08-09，commit 前最后一跑；110 基线＋4 评审补例）；② 真实 GitHub API 留痕：态③ octocat/Hello-World 动态入池 source='follow'＋基线快照落库＋重复关注幂等＋取消后跟踪保留（测后库已还原 repos=512/follows=0）；③ 真实服务 curl 全链路（RADAR_JOBS_ENABLED=0，:8013）：态① 200/alive、重复 POST already_followed=true 幂等不重复渲染卡、非法名 400、DELETE removed=true 且重复取消幂等、首页关注卡随操作出现/消失、星标 on/off SSR 持久全对。
- 独立评审结论：explore 评审 agent（primary，agent-15）0 高 1 中 4 低——中-1 态②复活不更新 node_id（已修：ingest_followed_repo 比对库内与 GitHub 现值，不一致 fail-loud ValueError→409，挡"同名删除重建"带旧 ID 复活次日被静默再标 dead；docstring 同步）、低-1 repos+基线与 follows 非单一事务（**留痕不修**：失败窗口残留"已入池未关注"符合决策 9 方向、非脏数据，重试走态①幂等补 follows 零 GitHub 消耗，状态收敛，S 档可接受——评审复审同意）、低-2 幂等短路 dead 仓标 STATE_REVIVED 语义误导（已修：只加注释不改行为——state 仅展示/留痕，already_followed=True 才是真语义）、低-3 测试缺口（已修＋4 用例：服务层同名重建 fail-loud 回滚、态②走 API 复活、改名/同名重建两情形 409、非字符串 as_of/空 body/非 JSON body 400）、低-4 前端双击无 in-flight 去重（**观察项留痕不修**：服务端 INSERT OR IGNORE＋幂等 DELETE 兜底，最坏多一个 toast，无数据错误——评审复审同意）。原评审 agent 复审确认 0 高 1 中 4 低全部清零、delta 未引入新问题、测试断言与实物一致。
- commit 号：92069aa
- 复验步骤卡：机器部分——`powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1`，预期 `All checks passed!` ＋ `114 passed`（已跑绿）；页面走查部分随交付消息给本人（四格尺感知类属本人保留验收项）。
- 阻塞原因：无

### T-015
- 根因/已确认事实：变更（非 bug）——D4 翻案，本人 2026-08-09 走查 T-009 后提出并拍板："我的关注"由周报页顶部区块改为独立页 P6（`/follows`）＋顶栏导航入口，关注多个时顶部区块侵占首屏、横滚查看效率低。冻结规格：《交互流程说明》v1.3（已确认）＋示意图 v4（已确认）。规格钉死：P6 按语言 7 组（classify_language 同口径，空组不渲染）、组内按当期周报窗口当周增量降序、组间按组内最大增量降序（v1.3 同日勘误）；行形态与榜单行一致（紧凑行＋详情默认展开＋标签区只读＋★）；三态：正常增量 / 新入池"—— 下周起有数据"沉组尾 / dead 灰显"已失效"沉组尾；顶栏 5 项"我的关注"带计数徽标、SSR 各页一致、当前页 active 态；P1 移除顶部关注区（五层→四层）；关注/取消后计数徽标前端局部 +1/-1（不再往页面插卡）；P6 内点 ★ 取消该行即时移除；空关注空态引导文案＋回首页链接（v1.3 §4）。既有事实：`_follow_cards`（routes.py:248）已按 repo_ids 过滤算增量可复用；`compute_repo_deltas` repo_ids 参数已在 T-009 就位；关注 API 三态/幂等/409 映射已验收（T-009），本次只动展示层。测试适配：test_follows.py 三处 card_html/首页关注卡断言随形态变更改写（流程变更适配，非放松标准）；test_web.py 补 P6 用例。
- 不改什么：关注/取消 API 的服务端三态逻辑、幂等、400/404/409/502 映射一行不动；榜单口径、schema、采集层不动；标签保持只读（T-010 接线）；不引新依赖；视觉体系（radar.css 设计 token）不动，只加 P6/导航徽标必需的样式。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest 120 passed（2026-08-09，commit 前最后一跑；114 基线＋6 个 P6 新用例，三处 card_html 断言随流程变更等价改写）；② 真实服务 curl 回归（临时库 :8016，真实库未碰）：空关注 /follows 200＋徽标 0、POST 关注后 /follows 出行＋徽标 1＋响应无 card_html、DELETE 后回空态徽标 0、/quarter、/total 徽标一致；③ 真实页面截图自审（临时库造 7 关注覆盖 6 语言组）：P6 分组/组头"N 个关注"/na 沉底/金★ on/顶栏 active＋徽标、P1 关注区移除＋徽标联动，全部正确（Chrome headless 留痕）。
- 独立评审结论：explore 评审 agent（primary，agent-17）0 高 0 中 4 低——低-1 死 CSS 残留（已修：fcard/follow-row 全家及媒体查询/hidden 兜底段删除，复审确认双向干净）；低-2 组间排序纯 na/dead 组键取 0、全负增量组沉后（**留痕不修**：触发需整组当周负增量、仅影响组次序，实现已 docstring 文档化）；低-3 总星页断言弱化（已修："fcard" not in → "follow-boards" not in，恢复对"关注区仅属 P6"的回归保护）；低-4 P6 移除行后 rank 不重排出空洞（**留痕不修**：规格只要求即时移除，cosmetic，刷新自愈）。原评审 agent 复审确认 4 条全清零、delta 无新问题，结论可 commit。
- commit 号：133794f
- 复验步骤卡：机器部分——`powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1`，预期 `All checks passed!` ＋ `120 passed`（已跑绿）；页面走查部分随交付消息给本人（四格尺感知类属本人保留验收项）。
- 阻塞原因：无

### T-011
- 根因/已确认事实：新功能（高风险：AI 生成内容入库，写 repos.description_zh 与 recommendations 两表）。规格钉死：《架构决策记录》决策 5（懒翻译：只译上榜项目、中文描述跳过、需要展示时才译存库）＋决策 6（推荐理由按项目生成一次、跨榜复用、每周重新生成）＋共识 §7（推荐语 2~3 句中文，输入：仓库名/简介/语言/本周增星/所属分类；prompt 工程授权 AI 决定）。展示侧已就绪：routes.py `_display_maps` 只读三表、NULL 降级渲染（流程说明 §4：无翻译只显英文、无推荐语该行无推荐语块、榜单照常出）。**触发点钉死（主 agent 定）**：每日 job（jobs.py `daily_job`）`run_daily` 之后调 `ensure_weekly_ai`——当周 17 榜（`compute_boards(period="week", top_n=30)`，与首页当前周同口径）去重项目集 S：① S 中 `description_zh IS NULL` 且 description_en 非空无 CJK → 翻译回填（译一次不再更新）；② S 中 recommendations 缺 (repo_id, 当周 ISO 周标签，格式 `2026-W32` 与 routes.py `_week_label` 同口径) → 生成 INSERT（同周已存在跳过＝幂等；跨周自然新行＝"每周重新生成"）。DeepSeek：POST api.deepseek.com/v1/chat/completions，model=deepseek-chat，httpx（已在依赖，不引 openai SDK），key＝`get_settings().deepseek_api_key`（.env 已配 DEEPSEEK_API_KEY）；key 空 → ensure 记日志返回零统计不抛（降级）。**试金石留痕：T-010 评审出 1 中 findings，按 2026-08-09 路由拍板本任务回退 k3 实施；真实 DeepSeek API 调用由主 agent 亲跑留痕，子 agent 全 mock。**
- 不改什么：report.py 榜单计算一行不动；展示层 routes/templates/static 不动；schema 不动（recommendations 表已建好）；GitHub 采集链路（collector/discover）不动；不引新依赖；AI 失败永不阻断 daily_job 主流程（ensure 调用整体 try/except 记日志；单项目失败跳过计入统计）；现有 test_jobs.py 断言不许放松。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` → ruff All checks passed! ＋ pytest **153 passed**（2026-08-09，132 基线＋18 实施用例＋3 评审补例；原 401 用例参数化适配扩展不占新增数）；② **真实 DeepSeek API 留痕（主 agent 亲跑，协议类必须真实执行）**：当周榜首周缺席为空（部署 <7 天），ensure 整轮真实跑会零调用，故以真实库总星 Top1（codecrafters-io/build-your-own-x，★537903）真实数据直调 translate＋recommend 各一次——translate 返回"通过从零重建你喜爱的技术来掌握编程。"（无引号包裹/无前缀），recommend 返回 3 句中文推荐语（首句说清是什么＋结合总星/分类说为何值得关注），prompt 输出形态与钉死口径完全一致；脚本与输出留痕于 `data/t011_real_api.py` / `data/t011_real_api_out.txt`（gitignored，验收后可清理）。
- 独立评审结论：explore 评审 agent（primary，agent-21）**0 高 0 中 4 低，放行**——低-1 auth 直通无测试锁定（已修：`test_auth_error_passes_through`）、低-2 部分失败跨轮收敛无测试（已修：两例断点续跑收敛——推荐失败轮零翻译调用补齐推荐 / 翻译失败轮只重译该条推荐零调用）、低-3 402/403 账户类错误未直通（已修：`status in (401, 402, 403)` 并入 DeepSeekAuthError 直通，401 用例参数化适配；复审附注两处行内注释已收窄为"账户类"）、低-4 共识 §7 措辞漂移（**登记待拍板不改文档**，见下）。原评审 agent 复审确认低-1/2/3 清零、低-4 处置路径认可（登记落盘即清零）、delta 无新问题。**待本人拍板（低-4 登记）**：共识文档 §7 原文"首次进入任一展示面（周榜/季榜/总星榜/被关注）才翻译、中文字符占比过半则跳过翻译直接存原文"，实现按任务书收窄为"当周 17 榜去重集＋任一 CJK 汉字跳过保持 NULL（展示回退英文原文）"。收窄依据：本人拍板"只翻译上榜的"＋决策 6 推荐语本就只有周榜语境；季/总星榜热门项目与当周周榜重叠度高，未覆盖项目显示英文降级形态（流程说明 §4 已钉）。建议：勘误共识 §7 适配实现（推荐）；或扩展 ensure 覆盖季/总星榜（成本仍 <¥1，但与"每周重新生成"语境错位）。**本人拍板前按实现口径执行。**
- commit 号：94556b2
- 复验步骤卡位置：机器部分——`powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify.ps1`，预期 `All checks passed!` ＋ `153 passed`（已跑绿）；推荐语/翻译实际效果属本人明日走查（8月16日首份周报生成后可见，或走查时看 data/t011_real_api_out.txt 留痕样本）。
- 阻塞原因：无

### T-016
- 根因/已确认事实：新功能＋变更（高风险：改已验收的 T-006 采集层与 T-011 AI 层，动公共组件 _row.html/radar.js）。规格全部冻结：共识 §7 v3（2026-08-10 拍板）＋架构决策 5 v2＋《交互流程说明》v1.4 §7（2026-08-10 确认）。钉死点：① 采集层每日快照（discover.py `_snapshot_all`，GraphQL nodes 本已拉 description 字段）比对 description_en——变更→UPDATE 原文＋清 description_zh=NULL（当日 ensure 自然重译）；INSERT OR IGNORE 新仓路径不动；language/topics 漂移不处理（边界留痕）。② ai.py ensure 翻译段从"当周上榜集"扩为**全池未译（含 dead）**；推荐语口径（当周上榜集＋周标签幂等）一行不动。③ 手动：`POST /api/translate` 单个**强制重译**（覆盖现值；无简介/原文含中文→400 对应文案）；`POST /api/translate-missing` 批量**只补 NULL**＋in-flight 锁（重复触发 409）。④ 行内按钮按态文案（无译文"翻译"/有译文"重新翻译"），成功 toast"已更新翻译"＋面板中文即时局部替换不刷新；页脚"补译全部缺失"按钮；异常文案全按 v1.4 §7.3 表；AI 失败旧译文保留。
- 不改什么：schema 不动；推荐语生成逻辑不动；榜单计算/分类/关注/标签逻辑不动；T-011 降级链（key 空零统计/单条 WARNING 跳过/auth 直通整轮吞）不动；不引新依赖。
- 验证命令及结果：① `powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` 一次跑绿（2026-08-10 收口跑）：ruff All checks passed! ＋ pytest **168 passed**（153 基线＋15 新增——test_translate.py 12 例＋test_jobs.py 3 例；test_ai.py 1 例随全池规格等价改写，原"未上榜不译"断言与决策 5 v2 冲突非放松）；② 复验预演 **8/8 PASS**（DeepSeek 子 agent：同进程 uvicorn :8023＋dependency_overrides 假 AI＋Playwright DOM 断言，全程零真实 API、临时库未碰 data/radar.db），产物落盘 `data/t016_rehearse/`（rehearse.py 可复跑＋8 步截图＋result.json）；③ **k3 视觉抽验通过**（8 张截图逐张判读；05 toast 未入镜定性为截图时序问题——toast 4 秒自动消失且该图拍的是刷新后页面，DOM 断言＋07 同款 toast 入镜佐证）。
- 独立评审结论：explore 评审 agent（primary，agent-1）**0 高 0 中 4 低，放行**——低-1 jobs.py docstring 仍写"懒翻译"（已修，主 agent 顺手改）、低-2 ai.py"重查一次"注释陈旧（已修）、低-3 单个 key 未配置 toast 指引弱于批量（**留痕不修**：§7.3 未钉单个 key 分支，500 detail 已带修复指引）、低-4 两测试缺口（**留痕不修**：① in-flight 锁 500 路径锁再获取无回归用例——finally 已保证无死锁路径；② dead 仓原文变更检测不到——dead=0 才进 nodes 轮询，译文可能永久陈旧，属 §7.4 未覆盖的固有边界，知悉）。规格逐条符合性（§7.1/7.2/7.3/7.4＋详情卡四钉死点）全对；推荐语逻辑 diff 逐行核实一行未动；T-011 降级链原样。主 agent 抽查：diff 范围与交付清单一致、verify 重跑绿、真实库 zh 行数 0。**实施事故留痕（自曝）**：DeepSeek 实施子 agent 收尾冒烟时误对真实库 elastic/elasticsearch POST /api/translate，触发 1 次真实 DeepSeek API（约 ¥0.0001），已立即还原该行译文，主 agent 核对全库 zh=0 无残留。
- commit 号：a3fb18f
- 复验步骤卡位置：`data/t016_rehearse/步骤卡.md`（本人真实环境走查版：步骤 1/2/5/8 主路径必查，3/4/6/7 可选［预演已覆盖］；预估 10~15 分钟；预演差异表在卡内）
- 阻塞原因：无（已按 C 方案全流程跑通：DeepSeek 实施＋k3 评审一轮放行）
- 打回留痕（2026-08-11 本人复验）：步骤 1/2/8 通过；步骤 5 打回。诊断：**设计缺陷非真 bug**——/api/translate-missing 为单请求同步跑全池未译（本次约 440 仓 × 单次数秒 ≈ 20 分钟级），按钮仅"补译中…"无进度，本人无法判断是否在跑（库内 zh 443/512，证明点击确实在工作）；且共识 §7 本就钉"后台执行、防重复触发"，实现未对齐。外加本人拍板设计变更："补译全部缺失"按钮页脚→顶栏（页脚操作不便）。修复规格（主 agent 钉死，随修复回填 commit）：① 按钮移顶栏右端（base.html 全页面共用，文字钮样式沿用，footer 说明文字保留）；② 批量改后台任务——POST 202 立即返回＋asyncio.create_task worker（自开库连接＋模块级 `_make_ai_client` 工厂供测试 monkeypatch）＋GET /status 轮询＋按钮进度文案"补译中…（已补 X/T 条）"＋页面加载接管在跑任务；③ 文案新增（v1.5 钉）：完成且 failed>0 →"补译完成：新译 X 条，失败 Y 条"；④《交互流程说明》§7 → v1.5；⑤ 复验只重走步骤 1/5。
- 打回修复收口（2026-08-11）：commit **2275a14**（代码＋流程说明 v1.5；T-017 需求留痕文档按节奏未混入）。verify 171 passed（169＋2 评审补例）；预演重跑 8/8 PASS（截图/result.json 刷新）。k3 评审 0 高 2 中 6 低——中-1 worker 启动段 get_conn/造 client 未纳入 try（失败终态永不落盘卡死批量，已修＋测试锁定含 task.exception() 断言）、中-2 双 margin-left:auto 挤偏 switcher 偏离示意图基线（已修：`header.top .week-switch ~ .translate-all` 特异性反超；实施方对任务书选择器失效的偏差自曝，复查判定成立）；低-1 轮询遇重启误报完成/低-3 预演步骤 6 留痕不自洽/低-4 两测试缺口（unknown 分支＋二次 POST 重置，已修）；低-2 fixture 跨线程 cancel/低-6 worker 写库不重查现值竞态（**留痕不修**：低-6 并入 T-017 worker 收窄重写顺带收口）；复查新增低-1 窄屏 switcher 页按钮左对齐（主 agent 直修媒体查询一行）。原评审 agent 增量复查 6 项全清零、偏差认可、结论可 commit。**验收回执：2026-08-11 本人复验步骤 1/5 通过（前次 1/2/8 已通过），T-016 验收完成。**

### T-017
- 根因/已确认事实：变更（高风险：改已验收 T-011 推荐语与 T-016 翻译，动 schema/公共组件）。2026-08-11 本人拍板三点：① 推荐语覆盖三口径榜（周/季/总星 Top30 去重）∪ 关注集（关注未上榜生成**总星维度**文本，P6 用——2026-08-11 复验讨论修订：关注页是长期盯梢语境非热点语境，周文本对低增量仓牵强且每周重刷纯 churn）；② 分维度（B 方案）——周/季文本带增量语境（输入含当期增量："本周/本季新增 X 星，为什么火"）、总星文本存量语境（输入总星/分类："是什么＋领域地位"，evergreen）；③ 翻译范围翻案收窄上榜∪关注（决策 5 v2 全池 2026-08-10 拍板、2026-08-11 翻案，仅存活 1 天，理由：上榜/关注之外的仓永不进入视野，全池翻译在 6.4 万目标规模下是无谓开支）。补充：推荐语做行内单个＋页头批量按钮（与翻译同构）；按钮形态主 agent 建议**并列独立不共用**（语义/失败域/状态文案/节奏不同，共用无法表达"已译但无推荐语"等部分态；后台任务机制复用 T-016 形态），本人未提异议按并列执行。现状事实：recommendations 现 0 行（可再生，共识 §8）；_display_maps 全页面按当周周标签取推荐语（routes.py:355-362）；ensure 只打当周 17 榜（ai.py:219）；T-016 批量后台任务机制（_batch_state/create_task/status 轮询，修复中）可直接仿形。节奏：周/季文本每周重生成（决策 6 原节奏延续）；总星文本懒口径（缺时生成＋简介变更重生，不每周重刷）；每日 ensure 补齐范围内缺口（缺译＋缺各维度文本）。展示映射：周页→周文本当期周标签；季页→季文本当期季标签；总星页→总星文本最新一条；P6→总星文本最新一条（2026-08-11 修订）；缺则该行无推荐语块（现降级不动）。schema：recommendations 重建为 `(repo_id, dimension['week'|'quarter'|'total'], period_label[周标签/季标签/'all'], text, readme_sha NULL, generated_week, PRIMARY KEY(repo_id, dimension, period_label))`＋索引 `(dimension, period_label)`；幂等迁移（PRAGMA 探列，已迁移则跳过；旧行若有则 dimension='week'/period_label=report_week 映射搬入，现 0 行无损、可再生数据）。总星文本 README 变更触发：行存 readme_sha（README blob sha 指纹），每日 ensure 比对、变化即当日重生总星文本（周/季不随此触发；sha=NULL 仓后续出现 README 视为变更自愈）。季榜 90 天窗口（约 11 月才出），季维度生成自然为空、实现照做。变更刷新口径（2026-08-11 本人确认）：简介变更（description_en 变化）→ 当日清除该仓全部维度推荐语（可再生数据，DELETE 随 T-016 译文清除同一检测点）＋当日 ensure 重生（范围内）；**README 变更（readme_sha 指纹每日比对）→ 当日重生总星文本**（周/季不随此触发——周文本随新周自然新行、季文本季内每周 REPLACE；2026-08-11 本人补充拍板）；总星文本 prompt 不引用具体星数/排名数字（防 evergreen 陈旧，口径级）；language/topics 漂移不处理维持 T-016 留痕边界（已入想法池评估）。翻译收窄影响面：ensure 翻译段与批量 worker 的 query 全池→上榜∪关注；手动单个翻译 API 不动（任何池内仓可手动译）；description_en 变更检测不动（清 zh 全局无害，范围内当日重译自愈）；已译 443 行保留不 purge。
- 不改什么：榜单计算/分类/关注/标签逻辑；翻译单个强制重译 API 与行内按钮形态；T-016 批量后台任务机制（仿形复用不改本体）；降级链（AI 失败不阻塞出榜）；schema 其余表；不引新依赖；推荐语输入改 **README 正文**（2026-08-11 本人拍板确认，共识 v4/决策 6 v2/流程说明 v1.6 已同步）：GitHub API 拉取截断入 prompt（空 README/拉取失败退化元数据输入，不持久化）；截断长度与拉取形态（GraphQL 批量/REST）授权实施；prompt 工程其余授权实施（维度语境钉死如上）。
- 验证命令及结果：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` 一次跑绿：**211 passed**（基线 171→207→211；含主 agent 脆测修复 1 例——原并发护栏测试为赛跑型，改双 Event 水闸确定性编排后 6 连跑全绿）。预演：`data/t017_rehearse/` rehearse.py **8/8 PASS**（16 截图＋result.json 落盘；季维度自然为空未预演，替代证据 pytest 季维度用例）；k3 视觉抽验 15/16 PASS 可放行（F3-1 截图时机证据缺口，DOM 断言兜底，留痕不修）。
- 独立评审结论：k3 初审 **1 高 2 中 6 低**——高-1 批量补译 pending query 未收窄上榜∪关注（已修：`_pending_translate_in_scope` 复用 `_scope_sets` 同源收窄＋测试锁）、中-1 README 拉取失败 sha=None 误重生＋清指纹 churn（已修：sha=None 不触发保旧指纹、单个 API 同口径、404 探测边界留痕）、中-2 无 switcher 页两批量按钮 auto margin 挤开（已修：`~ .recommend-all { margin-left: 0 }`）；低-6 项留痕不修（week 裸 INSERT 极小窗口、批量白拉 README、X≤T 毫秒窗口、existing=None 防御写法、单个重生 categories=[]、未来期次占位行边角）。k3 复审三项全清零、无新引入，结论**可 commit**（轮次 2/3）。
- commit 号：**9d65fe1**（代码 19 文件＋流程说明 v1.6；其余文档留痕按节奏未混入）；打回修复 **c599baa**（见下）
- 打回留痕（2026-08-12 本人复验）：总体没问题，但"周推荐语和总星榜推荐语没什么差别；从周推荐语生成后再点总星榜推荐语，两次都是一样的推荐语。不应该有两套/三套吗"。根因（主 agent 钉）：真实库快照仅 2026-08-09 单日 → 周/季榜全缺席 → 首页首期空态降级显示总星榜，按钮按"维度与展示一致"写 ('total','all')——本人两次操作写的是同一条 total 行（库内 13 行全 total/all、零 week 行证实）；设计本身三套齐全，周文本约 2026-08-14 滑窗满 5 天出真周榜后自然出现。修复（commit c599baa，DeepSeek 实施）：① 推荐语块标题维度标识（`_reason_label`：周榜推荐语 · 期次／季榜推荐语 · 期次／总星榜推荐语，模板/四个装配点/API reason_label/JS 局部替换同构）；② 周/季 prompt 钉死必须明确写出当期增星数字（与总星"不引用数字"肉眼可辨，delta 缺席退软要求）；流程说明 v1.7 同步。验证：verify **214 passed**；k3 增量评审 1 高（我新写的周页测试违反时间稳健约定、08-16 必红——DeepSeek 按评审钉死修法改历史周页＋种子固定期次，F3-1 docstring/F3-2 季标题断言顺带清零，F3-3 留痕）复查清零；预演重跑 8/8 PASS＋k3 视觉抽验 9 张全 PASS。**教训留痕：本次打回修复首轮主 agent 越权自实施（7 文件），违反"实施类一律 DeepSeek 起手"路由，本人指正后改派单；"主 agent 直修"仅限一两个文件的小修。**待本人二次复验（步骤卡已更新"当前真实库状态说明"）。
- 复验步骤卡位置：`data/t017_rehearse/步骤卡.md`（本人走查版：真实库＋真实 key；步骤 1/2/4/5/8 主路径，3/6/7 可选分支预演已覆盖；2026-08-12 已更新块标题预期与真实库状态说明）
- 阻塞原因：无（T-016 已收口 commit 2275a14；《交互流程说明》§8 v1.6 本人已确认 2026-08-11）。
- 验收回执：2026-08-12 本人二次复验通过（首轮总体没问题、打回一项"周/总星推荐语没差别"已修复清零），T-017 验收完成。

### T-018
- 根因/已确认事实：变更（高风险：改已验收 T-007 榜单口径与 T-008 页面，联动 T-017 推荐语输入）。规格钉死：《架构决策记录》决策 4 v2（2026-08-12 本人拍板，季榜年轻项目盲区讨论选 A 方案＋周榜同病同构）。钉死点：① 准入——周/季主榜出席校验失败（在池跨度不足最小窗口：周 <5 天、季 <86 天）且有端点快照的仓进新崛起区；② 口径——在池增量＝端点星数 − 入池基线快照（该仓最旧一张）星数，逐行标注"入池 N 天增 X"；③ 排序展示——在池增量降序、每分类榜 Top 10、分区展示不与主榜混排、空区不渲染；④ 毕业——在池跨度满最小窗口当期起出席主榜、自动离开新区（自愈无维护）；⑤ AI——新区行属上榜口径：翻译/推荐语覆盖沿用上榜∪关注，推荐语用对应维度（周榜新区→week、季榜新区→quarter），prompt 增量语境传入池天数与在池增量（区别于主榜窗口增量语境）；⑥ 新区不进"我的关注"页（P6 语义不变）。现状事实：report.py `_increment_delta` 缺席返回 absent_reason=ABSENT_FIRST_WEEK，可扩展携带在池增量信息；入池基线快照＝star_snapshots 主键 (repo_id, captured_at) 该仓最旧一张；全池 512 仓 2026-08-09 入池——86 天内季榜主榜全空，新区是冷启动期季榜唯一内容。口径提醒（2026-08-12 讨论定）：新区混有"GitHub 新项目"与"新入池老仓翻红"（repos 无 GitHub 创建时间字段，不区分、不加该字段）。
- 不改什么：主榜三口径计算逻辑（`_increment_delta` 出席路径一行不动）；schema 不动；总星榜不设新区（无增量概念）；关注/标签/翻译既有逻辑不动；不引新依赖；《功能闭环清单》/《交互流程说明》对应条目开工时先补。附带提醒（2026-08-12 k3 复评 F3-6）：开工触碰 report.py 时顺手回刷 :32 注释——"季 86~94 天＝90±4 是实施自定参数（冻结物只钉'季榜 90 天同口径'）"自决策 4 v2 起不再成立（窗口已被冻结物钉死）。
- 验证命令及结果：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` 一次跑绿 **233 passed**（2026-08-13；基线 214→232→233，+19 用例：test_report 11［含 EXPLAIN 锁基线 SQL、straddle 两不见］＋test_web 5＋test_ai 3；test_ai/test_recommend 16 例计数断言随季榜新区入集规格必然更新、test_web 2 例随降级判定变更等价重写，均非放松——k3 逐处核验认可）。预演：`data/t018_rehearse/` rehearse.py **9/9 PASS**（临时库造数 9 仓×2 库、:8124、零真实 API、未碰真实库；11 截图＋result.json 落盘）；k3 视觉抽验 10 张 PASS＋抓 1 断言外缺陷（375px 增量列词中断三行，CSS 修复 commit 8e6054e，真容器截图复核单行生效；09b 克隆特写失真留痕——截图方法教训：克隆脱离 .rising 上下文选择器不命中）。
- 独立评审结论：k3 初审（agent-19）0 高 **1 中** 5 低——中-1 准入实现宽于冻结文字（出席失败全集含停机 >9 天 straddle 老仓进新区，区头"入池不足 7 天"失真）：**主 agent 拍板收窄对齐冻结字面**（仅 pool_days < win_min 进新区，跨窗空洞老仓回 v1 两不见＋全空走既有降级；宽解读 a 被否——与冻结文字一致、缺席语义干净、复用 v1 降级路径；本人复验可翻案）；低-1 模块标题（已修）、低-2 test_web 混合行尾（已修：字节级统一 LF）、低-3 round 银行家舍入（留痕：x.5 概率极低）、低-4 入池 0 天仓 prompt"入池 0 天新增 0 星"（留痕：规格直接后果，复验看实际效果）、低-5 row 可空化无 assert（已修：两处加固）。复审（agent-19 增量）全清零＋新 2 低（注释伪证明"二分钱无遗漏"——实际存在 pool_days 落窗内第三类缺席［基线合法但非最近起点候选］，已改写正确表述；RisingRow/Board docstring 超集描述，已补 gate 半句），主 agent 直修后结论**可 commit**。
- commit 号：**24d967b**（代码 9 文件＋流程说明 v1.8＋闭环清单）；**8e6054e**（增量列 nowrap 视觉修复）
- 复验步骤卡位置：`data/t018_rehearse/步骤卡.md`（本人真实库走查版：步骤 1/2/3/5/6/7 主路径，4/8 真实库不可复现由预演覆盖，9 窄屏可选；含"当前真实库状态说明"——单日快照期新区行增量列显示 `+0（入池 0 天）` 为口径正常、推荐语/翻译缺失为预期降级）
- 阻塞原因：无

### T-019
- 根因/已确认事实：新功能（高风险：改已验收 T-006 采集层）。来源：想法池 2026-08-09 条目（T-006 独立评审低-2），2026-08-12 过表纳入。问题：发现池每日 updated 排序 ×500 条/天只覆盖活跃面，新涨到 ≥1000 星但近期无更新活动的仓库可能永久缺席。候选（实施时定并留痕）：每周一次 created 排序查询／星数区间轮换查询补捞。约束：Search 单查询 ≤1000 条上限不破；配额可控（每周一次量级小）；补捞入池即抓基线快照（同 discover 既有口径）；新入池 source 值新设或复用实施时定。
- 不改什么：每日发现池主流程、核心池每日快照、dead 标记逻辑不动；不引新依赖。
- 验证命令及结果：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1` 一次跑绿，252 passed（基线 246＋新增 6）。
- 独立评审结论：k3 评审零 F1/F2，可收口；F3×4（日志"实捞"措辞/ISO 跨年轮换限度/注释笔误"周六"→周日/run_daily docstring 未同步）——F3-1/F3-3/F3-4 单行措辞已顺手修，F3-2（ISO 跨年处个别波段间隔拉长，无状态方案固有限度）留痕不修。
- 真实执行留痕：当周波段（2026-W33 → bands[5]）`stars:32000..64000` dry-run 真实 API：total_count=732、Top 500 截取不破 Search 1000 上限、区间语法与降序正确（2026-08-12）。
- commit 号：e34583c
- 复验步骤卡位置：无页面（采集层），复验口径＝data/jobs.log 每周一 UTC 的"每周补捞执行"日志行（首跑 2026-08-17 周一）。
- 阻塞原因：无

### T-024
- 根因/已确认事实：新功能（高风险：新 AI 生成链路＋内容入库＋成本）。来源：想法池 2026-08-11 AI 总结（本人 T-016 复验提问"为什么只有翻译没有 AI 总结"）；README 正文输入链路已被 T-017 吸收（推荐语输入＋readme_sha 指纹每日比对），本条激活剩余形态，2026-08-12 过表纳入。**形态已拍板（2026-08-12 本人）**：详情面板 AI 概要——段落级"是什么/怎么组成"（README 文档视角），与推荐语（"为什么值得关注"，营销视角）并存不替代；概要无维度概念，全页面同一条。钉死：覆盖上榜∪关注（同 T-017 口径，10²~10³ 量级，非全池 6.4 万）；成本 ¥ 级/周；AI 失败降级不阻塞出榜；存储复用 recommendations 表或新增维度实施时定（动 schema 需幂等迁移）。
- 不改什么：翻译/推荐语既有逻辑不动（若形态定为"替代推荐语"则按变更管理另行钉死）；不引新依赖。
- 验证命令及结果：verify.ps1 一次跑绿，269 passed（基线 252＋新增 17）。
- 独立评审结论：k3 初审 F2-1（radar.js replaceReason 无推荐语块时新块会插到概要块下方，块序错乱）——主 agent 修复（插入点改 `.summary, .endpoint-note, .window-note, .acts` 首个命中块之前）；F3-1（prompt 多钉"怎么用"超冻结文本）已对齐；复审清零可收口。F3-2（"零新增 GitHub 请求"交付措辞失准，代码行为正确）/F3-3（sqlite_master 检测理论误判面，响亮失败非静默）/F3-4（两处适配断言精度略降有计数兜底）留痕不修。
- 真实执行留痕：①真实 DeepSeek 一次（codecrafters-io/build-your-own-x，输出 3 句中文、文档视角、无数字，合格）落盘 data/t024_real_api.txt；②真实库 schema 迁移（3 值 CHECK→4 值）13 行旧数据无损、索引重建、幂等（迁移前备份 data/radar.db.pre-t024）；③真实每日 AI 一轮：概要 325 条铺满 S 集（jobs.log 汇总行留痕）。
- commit 号：be45f5e
- 复验步骤卡位置：无独立步骤卡——任意页面行详情面板"AI 概要"块直接看（真实库已铺 325 条）；降级态（无概要行无块）已含在测试断言。
- 阻塞原因：无

### T-025
- 根因/已确认事实：变更（高风险：改已验收 T-006 采集层 GraphQL 查询与比对逻辑）。来源：想法池 2026-08-11（本人确认"项目变更能否自动识别刷新"时引出；T-016 已留痕为固有边界），2026-08-12 过表纳入。问题：每日采集只比对 description_en，language/topics 变更不刷新，影响分类归属与推荐语输入。方案：nodes 批量查询加 primaryLanguage/topics 字段，每日比对，变更即 UPDATE repos（topics JSON 序列化口径同既有写入方）。**实施时定并留痕**：变更后归类即时生效（分类实时计算不物化，自然生效）；language/topics 变更是否触发推荐语重生的联动口径。
- 不改什么：每日任务主流程、dead 标记、发现池逻辑不动；不引新依赖；两字段对 GraphQL 配额的实际影响真实执行一次留痕。
- 验证命令及结果：verify.ps1 一次跑绿，246 passed（基线 239＋新增 7）。
- 独立评审结论：k3 评审零 F1/F2，可收口；F3×4（模块 docstring 汇总枚举漏"漂移"已顺手修/topics 未漂移时被重排重写——影响面≈零留痕不修/脏 topics JSON 批次爆炸半径属 fail-loud 既定口径留痕/补测缺口可选不补）。
- 真实执行留痕：2026-08-12 真实一轮每日任务：快照 512 行、新发现 404、漂移更新 10 个、零 ERROR（data/jobs.log 汇总行留痕）。
- commit 号：ba8638e
- 复验步骤卡位置：无页面（采集层），复验口径＝data/jobs.log"每日任务汇总"行的"漂移更新 N 个"。
- 阻塞原因：无

## 当前速览·历史沿革

- T-001~T-005 已通过。批次一完结。
- T-006 已通过（commit 28b4498；node_id 勘误已报备留痕）。
- T-007 已通过（85 passed＋真实库三口径 17 榜跑通＋EXPLAIN 索引 seek 留痕，commit 4fdce5e；评审低-1 已入想法池）。
- 批次二完结。
- 示意图 v3＋流程说明 v1.1 已冻结（commit 1c30560，默认全部展开+独立折叠）。
- T-009 已通过（本人页面走查通过 2026-08-09，commit 92069aa；评审 0 高 1 中 4 低全清零，低-1/低-4 留痕不修）。
- T-015 已通过（本人页面走查通过 2026-08-09，commit 133794f；评审 0 高 0 中 4 低全清零，低-2/低-4 留痕不修；"P6 也要分类"新想法已入想法池，与左侧可隐藏导航栏合并处理）。
- T-010 待验收（commit eba66e6，132 passed；DeepSeek 实施，评审 0 高 1 中 3 低全清零——**试金石判定：出 1 中 findings，按 2026-08-09 路由拍板，T-011/T-012 实施回退 k3**；评审中-1 为打标成功后"＋ 标签"按钮不刷新无法连续打第二枚，已修复并复审清零，JS 行为无测试覆盖、走查时建议打两枚标签不刷新验证）。
- T-011 待验收（commit 94556b2，153 passed；k3 实施＋k3 评审 0 高 0 中 4 低全清零；真实 DeepSeek API 留痕形态正确；**低-4 共识 §7 措辞漂移登记待本人拍板**，详见详情卡）。
- T-010 已通过（本人复验步骤卡 A/B 全过 2026-08-10，含修复点"不刷新连续打两枚"；两条标签优化想法已入池）。
- T-011 已通过（本人样本过目 2026-08-10，data/t011_real_api_out.txt 形态认可；**低-4 共识 §7 勘误仍待本人拍板**，详见详情卡）。
- T-016（批次四追加，高风险）已立项：翻译增强（全池覆盖＋原文变更自动重译＋手动单/批量触发），2026-08-10 本人拍板；共识 §7 已重写 v3＋架构决策 5 已改 v2；《交互流程说明》§7 v1.4 已确认（2026-08-10，组件复用免示意图）。
- T-016 待验收（commit a3fb18f，168 passed；DeepSeek 实施＋k3 评审 0 高 0 中 4 低放行——C 方案首件跑通；复验预演 8/8 PASS＋k3 视觉抽验通过，步骤卡 data/t016_rehearse/步骤卡.md 待本人走查；实施方误触 1 次真实 API 事故已还原留痕）。
- T-016 已通过（本人复验步骤 1/5 通过 2026-08-11，commit 2275a14；打回修复评审 2 中 6 低全清零）。
- T-017 待验收（commit 9d65fe1＋打回修复 c599baa，214 passed；DeepSeek 实施＋k3 评审两轮 1 高 2 中全清零、低-6 留痕不修；2026-08-12 本人复验打回一项"周/总星推荐语没差别"——根因＝真实库单日快照周/季榜缺席、首页降级显示总星榜，两次生成写的同一条 total 行，非缺陷；修复＝块标题维度标识＋周/季 prompt 钉死当期增星数字，k3 增量评审清零、预演重跑 8/8＋视觉抽验放行；步骤卡 data/t017_rehearse/步骤卡.md 已更新，待本人二次复验）。
- T-008 已通过（本人页面走查通过 2026-08-09，commit bb83d89；URL 形态/P3 分段控件/窗口行级标注三项勘误拍板，流程说明 v1.2 已回改；左侧可隐藏导航新想法已入想法池）。**本人拍板留痕（2026-08-09）**：① D4 翻案——"我的关注"改独立页 P6＋顶栏导航入口、按语言分组（流程说明 v1.3＋示意图 v4 已确认），登记为 T-015，先于 T-010 执行；② 文档提交节奏——代码 commit 后文档不立即单独 commit，等本人反馈后一起提交；③ 示意图分档——组件复用型变更免示意图、流程说明确认后直接开发（已写入 AGENTS.md 工程约定）。T-012/T-013 进行中（起草产物 commit 981216d；**阻塞：待本人提供服务器 SSH 访问方式与域名指向**）。试金石结论已拍板（2026-08-10，C 方案）：实施类全部 DeepSeek 起手，每任务最多三轮 k3 评审，三审仍有中/高则 k3 接手该任务（不牵连后续）；T-011/T-012 已回退 k3 实施不回刷。T-017（批次四追加，高风险）已立项（2026-08-11 本人拍板）：推荐语扩三口径榜 ∪ 关注集＋分维度（B 方案：周/季增量语境、总星存量语境）＋行内/页头手动生成按钮（与翻译同构、并列不共用）；翻译范围翻案收窄上榜∪关注（决策 5 v2 全池仅存活 1 天）；T-016 已收口不阻塞。同日复验讨论修订：关注页/关注集改总星维度＋推荐语输入改 README 正文（截断、空/失败退化元数据）——§8 v1.6 本人已确认（2026-08-11，含 README 变更触发总星文本当日重生）。T-017 已通过（本人二次复验通过 2026-08-12，commit 9d65fe1＋c599baa）。"为什么只有翻译没有 AI 总结"新想法已入池（2026-08-11，同日被 T-017 README 输入部分吸收）。下一步：服务器信息（SSH 访问方式＋域名指向）到位 → T-012/T-013 生产执行；T-014 挂部署后 7 天观察；版本验收 v0.1（验收记录.md）挂 T-014 后。

# 架构决策记录（阶段④冻结物，S 档一页技术摘要）

> 规则：每条决策必须附理由；本人能用自己的话复述，才算确认。
> 确认状态：【已确认】　确认日期：2026-08-09　版本：v1　基线：dev-sop v1.4

## 1. 主栈一览
- 前端：Jinja2 服务端渲染 + 少量原生 JS（无前端框架）
- 后端：Python 3.12+ / FastAPI + httpx（async）+ APScheduler（进程内调度）
- 数据库：SQLite（WAL 模式，单文件）
- AI：DeepSeek API（deepseek-chat）
- 部署：云服务器 systemd 托管 uvicorn；Caddy 反代（已有域名，自动 HTTPS + basic auth）

## 2. 数据模型概要
- `repos`：仓库主表（id, full_name, description_en/zh, language, topics JSON, dead 标记, 入池来源/时间）；中文翻译懒写入本表
- `star_snapshots`：星数快照（repo_id, captured_at, stars），主键 (repo_id, captured_at)，每日每仓库一行
- `follows`：关注（repo_id, created_at）
- `tags`：自定义标签（repo_id, tag）
- `recommendations`：推荐理由（repo_id, report_week, text），按周重新生成
- 榜单**实时 SQL 计算**（两快照差值排序），不做物化表；慢则加缓存（已入想法池候选）

## 3. 决策记录
| # | 日期 | 决策内容 | 理由 | 被否决的备选 | 影响范围 |
|---|------|----------|------|--------------|----------|
| 1 | 2026-08-09 | 数据源用 GitHub 官方 API（GraphQL 批量为主，Search 仅发现/初始分片） | 稳定性；GraphQL `nodes(ids:)` 一次拉 100 仓库，6.4 万核心池每日仅 ~640 请求 | REST 逐仓库拉取（配额紧张）、Trending 爬虫（易碎） | 采集层 |
| 2 | 2026-08-09 | 分层跟踪池：≥1000 星核心池 + 每日发现池 | 实测 ≥100 星有 47.2 万个，每日全量更新超 REST 预算 4 倍，物理不可行；定位不追黑马，迟到可接受 | 全量 ≥100 星跟踪（不可行） | 全局 |
| 3 | 2026-08-09 | 分类全部本地 SQL 计算；主题用封闭词表 YAML 匹配 topics | 零 API 成本，词表可配置、归类每日可全量重算；"等"字无法进代码必须封闭 | Search API 按分类查询（配额浪费、NOT 语义不可靠）、AI 归类（几万元成本不值） | 分类/榜单 |
| 4 | 2026-08-09 | 周增量 = 两端快照差；端点缺失滑动窗口（5~9 天）并标注；新入池首周缺席 | 星数单调只看两端，天然消化"3 天静默容错"；首周无基线且无法回填，缺席比混排口径更诚实 | 缺失即中断报警（违背静默原则）、首周按实际天数混排（误导） | 榜单口径 |
| 5 | 2026-08-09 | 懒翻译：首次进入展示面才翻译存库；中文描述跳过 | 初始翻译量从 6.4 万条降到几百条，DeepSeek 成本 < ¥1；未上榜项目永远没人看 | 入库即全量翻译（6.4 万条白花） | AI 层 |
| 6 | 2026-08-09 | 推荐理由按项目生成一次、跨榜复用、每周重新生成 | 同一项目多榜出现时提示词几乎相同，分开生成纯浪费；每周刷新保持与本周热点语境一致 | 按"项目×分类"生成 510 次/周 | AI 层 |
| 7 | 2026-08-09 | SSR + SQLite + 进程内 APScheduler | 单人内部工具，无前端框架/无独立调度进程，进程最少、部署最简 | 前端框架（过度）、PostgreSQL（过度）、系统 cron（失去进程内状态可见性，可复议） | 全局 |
| 8 | 2026-08-09 | Caddy + Basic Auth，无应用内用户系统 | 写操作与 AI 触发必须挡外人；单人一个密码足够，Caddy 顺带自动 HTTPS | 纯 HTTP + basic auth（密码明文，不可接受）、自签证书（体验差）、Tailscale（已否决）、登录页/session（过度） | 部署/安全 |
| 9 | 2026-08-09 | 关注 = 动态入池：关注即抓基线快照并加入每日跟踪 | 否则关注的低星项目永远没有"当周增量"，功能承诺落空；+50 仓库配额无感 | 只显示总星（功能残废）、为关注单独维护第二套采集（重复） | 关注功能 |
| 10 | 2026-08-09 | 每日 `sqlite3 .backup` + gzip 本地轮转 14 份，每周 scp 异机一份 | 星数历史与关注/标签不可再生；`.backup` 是在线备份，直接 cp 可能拷出损坏副本 | 不备份（数据空洞不可接受）、只本地轮转（不防整机报废） | 运维 |

## 4. 已踩过的结构坑
- 新仓库，无历史坑。 grilling 阶段已排除的最大地雷：全量 ≥100 星跟踪假设（决策 2）。

## 附录：主题词表（YAML，已封板 2026-08-09）

> 本人已封板。事实源为仓库内 `config/topics.yaml`（与本附录一字一致，T-004 机器 diff 验证）；封板后增删词条须本人确认，并同步本附录与 `tests/test_classify.py` 的词数锁定断言（否则 verify 红——这正是防漂移设计）。

```yaml
# config/topics.yaml —— 命中规则：仓库 topics 含任一词条即进该主题；命中多主题多榜重复；零命中进"其他"
ai:
  label: AI与智能
  words: [machine-learning, deep-learning, artificial-intelligence, ai, llm, large-language-model,
          ai-agent, agents, nlp, natural-language-processing, computer-vision, neural-network,
          transformer, diffusion-model, rag, retrieval-augmented-generation, mcp, generative-ai,
          chatgpt, pytorch, tensorflow]
frontend:
  label: 前端/UI
  words: [react, vue, angular, svelte, nextjs, nuxt, tailwindcss, css, frontend, front-end,
          webassembly, wasm, ui, design-system, component-library, vite, threejs, webgl]
backend:
  label: 后端/云原生
  words: [spring-boot, nodejs, microservices, kubernetes, docker, serverless, api-gateway,
          graphql, grpc, backend, api, rest-api, cloud-native, helm, istio, kafka, rabbitmq]
data:
  label: 数据库与数据工程
  words: [database, postgresql, mysql, mongodb, redis, elasticsearch, data-engineering, big-data,
          spark, clickhouse, etl, data-pipeline, duckdb, sqlite, vector-database, olap, flink]
devops:
  label: DevOps/监控
  words: [devops, monitoring, logging, observability, terraform, ansible, ci-cd, prometheus,
          grafana, alerting, sre, infrastructure-as-code, opentelemetry]
devtools:
  label: 开发者工具
  words: [cli, code-quality, testing, static-analysis, ide-plugin, developer-tools, productivity,
          linter, formatter, debugging, vscode-extension, git, tui, terminal]
security:
  label: 安全
  words: [security, vulnerability, cryptography, pentesting, infosec, privacy, encryption,
          authentication, oauth, cve, malware-analysis, zero-trust]
game:
  label: 游戏/图形
  words: [game-development, game-engine, graphics, opengl, vulkan, unity, unreal-engine,
          godot, shader, raytracing, directx, pixel-art]
automation:
  label: 自动化/脚本
  words: [automation, scripting, workflow, bot, scraper, web-scraping, crawler, rpa,
          home-automation, cron, playwright, selenium]
```

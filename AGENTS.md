# AGENTS.md 项目规范（GitHub 热门项目雷达）

本文件与全局 `AGENTS.md`（用户级）**合并生效**：通用工作方式、任务路由、评审验证与安全通则以全局为准；本文件只定义项目特有规则与本人拍板条款。规则优先级：用户最新指令 > 本项目 `AGENTS.md` > 全局 `AGENTS.md`/`SDD.md`（全局口径：项目级约定优先于全局）；但任何指令都不能降低安全、Git、凭据与评审底线门禁。

## 1. 项目目标与边界

- 项目目标：为本人（单人使用）做一个全自动 GitHub 热门项目雷达——每日采集星数、每周产出分语言/分主题的增量榜单，用于扩展技术知识面、支撑项目选型参考。
- 规模档（本人拍板）：**S（轻量）**——判定信号：无真实外部用户（本人单人使用）；交互功能 3 个（榜单浏览/关注/标签，季度回顾与总星榜复用同一展示模式）；无数据迁移；不涉资金/权限/隐私（Basic Auth 为运维层挡外人，非应用权限体系）。下调/上调须本人拍板留痕。
- 核心链路（dev-sop 铁律评审门禁适用面）：每日采集入库 → 榜单计算 → 榜单/关注页展示。
- 技术契约体系：无（S 档单人项目，不引入 OpenSpec；技术契约以《共识文档》+《架构决策记录》为准）。

## 2. 产品层 SOP（dev-sop）

- 铁律与流程（风险分级验收、四格尺完成定义、独立评审门禁等，含 `references/` 各模块细则）以 dev-sop skill 原文现行版本为准，本文件不快照。
- 流程基线：dev-sop v1.4（各冻结物头部的对齐基线）。skill 现行版本高于基线时，AI 在会话开头对比基线差异、给出是否跟进建议，本人拍板后统一回刷基线字段。
- 文档地图（`docs/sop/`，本项目所有产品层决策的唯一事实来源；确认状态与版本沿革见各文档头部；冻结物以仓库内 `docs/sop/` 与本文件拍板条款为准）：
  - `需求清单.md` —— 为谁做、做什么、不做什么、成功标准（阶段①冻结物）
  - `功能闭环清单.md` —— 每个功能的闭环路径（阶段②冻结物）
  - `交互流程说明.md` —— 阶段③前置冻结物（流程与设计取舍以此为准）
  - `示意图.html` —— 阶段③冻结物（S 档一张示意图，主栈+假数据；页面视觉基线以此为准）
  - `架构决策记录.md` —— 技术决策及理由（阶段④冻结物，S 档一页技术摘要＋主题词表附录）
  - `任务拆解表.md` —— 开发任务清单与验收状态（阶段⑤冻结物）
  - `归档/` —— 已完结任务详情卡与速览历史沿革、已处理想法（归档只进不出，现行口径不搬）
  - `验收记录.md` —— 版本级验收走查记录（阶段⑥冻结物）
  - `共识文档.md` —— grilling 逐问确认的需求与技术共识基准（需求清单的详细来源）
  - `想法池.md` —— 未排期的新想法（开发中冒出的想法只进这里）
  - `服务器探测记录.md` —— 工作文档（不冻结；T-012 起草，上生产前探测命令清单，执行后回填转正式记录）
- 读取分级：会话必读＝需求清单、功能闭环清单、任务拆解表"当前速览"；按需读＝共识文档、架构决策记录、验收记录；事件触发＝想法池。
- 当前进度（任务状态、批次、阻塞、冻结物确认状态）以《任务拆解表》"当前速览"与《验收记录》为唯一准绳，本文件不复制状态快照。
- 项目拍板（dev-sop 细则的项目补充）：
  - 示意图分档：交互变更按"视觉新信息量"分档——有新视觉元素/新组件/新交互模式的，出示意图（或同保真原型）确认再开发；既有冻结视觉体系内组件的重组/搬迁/复用，免示意图，《交互流程说明》确认后直接开发，开发完以真实页面截图交付本人过目＋走查。文字层（流程说明前置确认）两档均不免。dev-sop skill 本体是否跟进此口径，留版本复盘再议。
  - 验收口径：机器可执行的工程验证类验收动作（verify.ps1 一次跑绿、pytest、可脚本化检查等）由 AI 直接执行验收并放行，无需本人逐步操作；保留本人验收的：页面/交互走查（四格尺感知类）、词表封板、拍板决策与冻结物确认。
  - 复验交付：交付本人走查必须给**规范复验步骤卡**——前置条件与状态变量控制、逐步操作精确到按钮文案、每步预期＋"没看到时的判型分支"、通过/打回标准、预估耗时；禁止用验收清单/复验首页概要替代。步骤卡交付前每一步必须在真实运行环境按字面预演跑通（浏览器自动化打真实服务），无法预演的步骤显式标注"AI 未预演＋原因"＋替代证据。预演执行与步骤卡起草派 DeepSeek 子 agent，机器产物必须落盘（截图＋result 文件）；视觉抽验（截图判读）与验收裁定仍 primary/本人。

## 3. 子 agent 模型路由（本人拍板，优先级高于全局同名条款）

- 实施类全部 DeepSeek（secondary）起手；评审一律 k3（primary）。
- 每任务评审最多三轮（k3 初审→DeepSeek 修→k3 复审→DeepSeek 修→k3 三审）；三轮后仍有未清零的中/高 findings 则 k3 接手**该任务**（不牵连后续任务，下一任务仍 DeepSeek 起手）。
- 低 findings 可留痕不修，不算"不过"。
- 图形/视觉判断（截图判读、视觉核对）一律 k3：DeepSeek 无 image_in，不做看图环节。

## 4. 验证与评审门禁

- 统一验证入口（收口只认它一次跑绿）：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1`（ruff + pytest）。
- 日常开发命令全集（本地起服务、依赖安装等）见 `docs/dev-commands.md`；新增或变更验证命令必须实际跑通后同步该文档。
- 评审门禁：按全局 `AGENTS.md` 任务路由表"评审"列与 dev-sop 铁律执行；独立评审子 agent 一律 k3（见 §3）。

## 5. Git 与文档提交节奏（本人拍板）

- commit message 用 Conventional Commits：`feat:` / `fix:` / `docs:` / `chore:` / `refactor:` / `test:` 开头，中文描述；历史 commit 不回刷。
- 文档提交节奏：代码 commit 后，文档改动不立即单独 commit——任务状态/验证留痕仍当场写入文件，但 git 提交等本人反馈确认后与后续改动一起提交。

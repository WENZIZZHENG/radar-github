# 项目约定（入口约定文件）

> 铁律与流程以 dev-sop skill 原文（SKILL.md）为准，本文件快照可能滞后；skill 更新后须同步检查。
> 流程基线：dev-sop v1.4（本项目冻结物以仓库内 `docs/sop/` 与本文件为准；skill 升级后，AI 在会话开头对比基线差异、给出是否跟进建议，由本人拍板。）

## 项目简介
为本人（单人使用）做一个全自动 GitHub 热门项目雷达：每日采集星数、每周产出分语言/分主题的增量榜单，用于扩展技术知识面、支撑项目选型参考。

## 文档地图
本项目所有产品层决策以 docs/sop/ 目录内文档为唯一事实来源：

- `docs/sop/共识文档.md` —— grilling 逐问确认的需求与技术共识基准（已确认 v2，需求清单的详细来源）
- `docs/sop/需求清单.md` —— 为谁做、做什么、不做什么、成功标准（阶段①冻结物）
- `docs/sop/功能闭环清单.md` —— 每个功能的闭环路径（阶段②冻结物）
- `docs/sop/架构决策记录.md` —— 技术决策及理由（阶段④冻结物，S 档一页技术摘要＋主题词表附录）
- `docs/sop/想法池.md` —— 未排期的新想法（开发中冒出的想法只进这里）
- `docs/sop/任务拆解表.md` —— 开发任务清单与验收状态（阶段⑤冻结物）
- `docs/sop/验收记录.md` —— 版本级验收走查记录（阶段⑥冻结物）
- `docs/sop/交互流程说明.md` —— 阶段③前置冻结物（已确认 v1，2026-08-09；流程与 4 项设计取舍以此为准）
- 效果图/原型（阶段③冻结物）位置：`docs/sop/示意图.html`（S 档一张示意图，主栈+假数据；v1 于 2026-08-09 被打回；v2 于 2026-08-09 按已确认《交互流程说明》重绘，已确认）
- 既有技术契约体系：无（S 档单人项目，技术契约以《共识文档》+《架构决策记录》为准，不引入 OpenSpec）
- 工作文档：暂无

> 读取分级：会话必读＝需求清单、功能闭环清单、任务拆解表"当前速览"；按需读＝共识文档、架构决策记录、验收记录；事件触发＝想法池。

## 铁律（违反任何一条视为任务失败；以 dev-sop skill 原文为准）
1. 开工先读与本任务相关的文档，再动手。
2. 上一阶段冻结物未经本人确认，不得进入下一阶段。
3. 新想法只写进 `docs/sop/想法池.md`，不实施；版本验收后统一处理。
4. 任务按风险分级验收：高风险（动数据结构或迁移、改已验收功能或公共组件、删除/权限类操作）逐个验收，打勾才能领下一个；低风险（独立新页面、纯样式、文档）可打包验收，每包上限 3 个且须同一功能内相邻任务；包内任一不合格整包打回，且下一包降级为逐个验收；风险级在任务拆解表中标注、本人确认后生效。豁免通道（合并验收）：本人可拍板对一批修复豁免逐个验收、改为合并验收，留痕＋每任务独立 commit/评审/全量测试；涉数据结构/迁移/权限/删除不适用。
5. 遇模糊先提问澄清，禁止猜着做；提问必须同时给出建议方案与默认处理口径（含"不同意可改"的回退）；代码/环境/日志/文档能自查的事实问题一律自查，只有三类问题可问本人——真实数据语义、使用意图、拍板决策。
6. 无权自行重构：须提交申请（症状/后果、影响范围、改动量、回滚方案），批准权限于三类情形——结构致功能无法闭环、新需求被明确挡死、性能安全问题；"不够优雅"一律驳回。
7. 重构批准后，受影响功能按四格尺重新验收。
8. 任务级"完成"＝工程验证全绿＋四格尺验收通过：能进入、能操作、有结果、能延续（刷新/重新进入后成果仍在且可继续使用）；版本级再挂发布就绪（按规模档）。"AI 自测不算数"＝不能以 AI/CI 结果代替本人验收，工程验证仍是必需证据。
9. 完成任何任务时，同步更新受影响的文档；文档与代码不一致时，以本人最后确认的版本为准。
10. 单线程规则：同一时刻只进行一条产品层新功能主线，不并行开工；已立项的既有批次、多会话分工的外部轨道、运维与部署轨道不受此限。
11. 异议义务：拍板/冻结前，AI 必须给出真实评估——同意或反对＋证据＋替代方案；最终拍板权永远在本人。
12. 独立评审门禁（有子 agent 能力的工具链）：高风险或核心链路变更（含修复与新开发）commit 前必须独立 agent 只读评审，findings 清零或留理由。

## 当前阶段
阶段①~⑤ 全部冻结物已确认（2026-08-09：①需求清单 v1、②功能闭环清单 v1、③交互流程说明 v1＋示意图 v2、④架构决策记录 v1、⑤任务拆解表 v1）。
当前任务：**T-004 分类器与主题词表**（批次一：数据底座；进度以《任务拆解表》当前速览为准）。
规模档：**S（轻量）**——判定信号：无真实外部用户（本人单人使用）；交互功能 3 个（榜单浏览/关注/标签，季度回顾与总星榜复用同一展示模式）；无数据迁移；不涉资金/权限/隐私（Basic Auth 为运维层挡外人，非应用权限体系）。判定已记入此，本人下调/上调须留痕。
验收口径（本人拍板 2026-08-09）：机器可执行的工程验证类验收动作（verify.ps1 一次跑绿、pytest、可脚本化检查等）由 AI 直接执行验收并放行，无需本人逐步操作；保留本人验收的：页面/交互走查（四格尺感知类）、词表封板、拍板决策与冻结物确认。

> 技术验证命令（T-001 起生效，均已实测跑通）：
> - 统一验证入口（收口只认它一次跑绿）：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1`（ruff + pytest）
> - 本地起服务：`.venv/Scripts/python.exe -m uvicorn app.main:app --port 8000`
> - 依赖安装：`uv venv .venv && uv pip install -r pyproject.toml --extra dev`
